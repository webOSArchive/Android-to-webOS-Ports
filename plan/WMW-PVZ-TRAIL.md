# WMW / WMW2 / PvZ HD / Amazing Alex — the full working trail (archive)

**Provenance.** This is the verbatim body of the `wrapper-spike-progress` Claude memory as it stood
on 2026-08-27, before that memory was compressed to current-state-plus-pointers. It is the
chronological log of the first four ports (2026-06-26 → 2026-08-26): every theory, the addresses,
the experiments that failed, and the corrections.

**Read it as a trail, not as truth.** Entries are newest-first and many were later refuted — each
one that was is marked in place (`❌`, `(superseded)`, `(refuted)`, `CORRECTION`). The corrected
conclusions are summarised in the memory and in `PORTING-PLAYBOOK.md`; this file exists so the
*reasoning* behind them is not lost, including the dead ends, which are the expensive part to
rediscover.

Kept in the repo rather than in memory because it is ~11,500 words: too large to load on every
recall, too valuable to delete. Detail for individual stages also lives in `plan/STAGE-*.md`,
`plan/PVZ-HD-menu-freeze.md` and `plan/AMAZING-ALEX.md`.

---


**TEMPLE RUN 2 (Unity+Mono) OPEN 2026-08-26 evening:** dies in Mono init (pc=ASCII digits). Fixed on the way, all GENERAL: memalign/pvalloc hooks (second-heap corruption), bionic sigaction/sigset translation, __pthread_cleanup_push/pop, robust crash handler (regs/lr, anon-lib resolution, lock-free), APKENV_TRACE_CALLS/APKENV_TRACE_FREE tracers. Next: fix tracer recursion, audit pthread_attr_t (24 vs 36 bytes) — see plan/TEMPLERUN2.md. Harness /var/apkenv2/play-tr2.sh (has APKENV_TRACE_CALLS set). Uncommitted.

**AMAZING ALEX HD (Rovio ka3d, modules/angrybirds.c) PORTED IN ONE PASS 2026-08-26** via PORTING-PLAYBOOK.md — playable with music on first launch; .ipk `com.apkenv.amazingalex` launcher-confirmed; everything committed 2026-08-26; trail `plan/AMAZING-ALEX.md`. Binary md5 e5abce55 hosts WMW+PvZ+Alex.

**════════ PvZ HD — ROOT CAUSE FOUND BY CONTRACT REVIEW (2026-08-26) — read this first ════════**
The whole 06-29 "fibre stall / loader gate" trail below was chasing the WRONG BINARY: `libpvz.so` is only the Airplay 4.x RUNTIME; the game is `assets/PvZ.s3e` (LZMA-Alone, Sexy framework). The freeze is a **host-contract gap in `modules/marmalade.c`**: PvZ's first-run "Enter your name" prompt → engine calls Java `AirplayView.getInputString(title,flags)` (s3eOSReadString, libpvz 0x25a9c) then spins `deviceYield(20)` until native `setInputText` fills `[state+0x154]` (0x23a94); the module never answered → "frozen menu". Evidence: the 06-29 log itself showed `[MARMCALL] getInputString` on the freezing tap. Fix built (answer with `APKENV_MARM_READSTRING`, default "Player") + always-on `[MARM-JNI] UNHANDLED …` tracer for every unimplemented engine→host call + Airplay `audioPlay(String,int)` signature fix. **CONFIRMED ON DEVICE 2026-08-26: user played Adventure mode** (menu→seeds→level). Device was re-flashed (webOS CE 3.1.0): harness rebuilt over **novacom USB** (user prefers USB; WiFi .88 unreliable): `/var/apkenv2/{apkenv,play-pvz.sh,libs/webos}`, apk `/media/internal/pvzhd.apk`, resources+music(+.ogg) `/media/internal/.apkenv/pvzhd.apk/`, log `/media/internal/apkenv-pvz.log`; `novacom run file:///bin/sh -- /var/apkenv2/play-pvz.sh` launches it (novacom `-c` args are mangled — run binaries directly). **AUDIO CONFIRMED (md5 059fb4ba): SFX via generateAudio post-mix pump + music; user: "playable and sounds great".** Rules learned: mixer must open 44100 stereo (device SDL_mixer won't resample/remix music; has ffmpeg MP3); `rm build/webos/*.o` after header changes (no dep tracking). Letterbox CONFIRMED (APKENV_MARM_LOGICAL=1280x800 → centered 1024x640, generic viewport_offset hack + bar clear; fixed the wallpaper strip too). PACKAGED as `com.apkenv.pvzhd` .ipk (142MB: apk + data tree seeded to /media/internal on first run + android/apkenv.env for launch settings; per-app log apkenv-<appid>.log) — **LAUNCHER-ICON LAUNCH CONFIRMED; user: "ready to share with the community"**; retitled "Plants vs. Zombies" v1.0.1. Pending: Stage D cleanup (strip pt5–pt7 false-premise code) + commit; for public release prefer the runtime-only .ipk form (bundled apk is copyrighted). See plan/PVZ-HD-menu-freeze.md §8-9. Full analysis + staged theories B–F + test protocol: **`plan/PVZ-HD-menu-freeze.md`**; deploy helper `apkenv/tools/deploy-tp.sh`. Also corrected: `suspendAppThreads` is called by Java's UI thread and parks the CALLER (not app threads) — pt5–pt7's resume model was inverted; strip that code in Stage D. Method for next games is written up in the repo: **`PORTING-PLAYBOOK.md`** (contract-first triage, instrumentation, subsystem lessons, build/deploy/package mechanics) — point there instead of re-deriving.

**════════ FRESH-START PLAN — PvZ HD menu freeze (2026-06-29 EOD) ════════**
*This entry supersedes the layered PvZ exploration notes below (kept as the detailed trail). Read this first.*

**VERIFIED:** PvZ HD (Marmalade/Airplay, `modules/marmalade.c`) boots, the loading screen + "tap to continue" work, and **TOUCH WORKS** (the continue tap advances; taps reach the engine). The blocker: the **main/pre-game menu freezes** — it's a **cooperative s3e-FIBRE stall**: the loader fibre finished loading (file-task count `[loader_mgr+0x20]`==0) but stays runnable, idle-yielding; the main-loop **state machine** (state var `[r6+0x44]`=5) never transitions out of "loading" to render the menu, so the engine spin-pumps the idle loader fibre forever (swap frozen, main thread R on `s3eDeviceYield`).

**RULED OUT — do NOT re-chase (each proven on-device this session):** touch delivery · app-thread suspend/resume (`stat`/count) · pthread worker threads (libpvz creates ZERO via pthread_create) · missing files/resources (strace: no open/stat at wedge) · GPU/kgsl fence · file-task queue (count=0) · s3e free-RAM check (`RAMPATCH` forced it pass → NO change) · treating `0x1f690` as a skippable gate (`GATEBYPASS` forced return-0 → SEGFAULT in cfree; it does real per-tick load work). Two fix hypotheses tried, both failed → **next steps must be DIAGNOSTIC, not speculative patches.**

**FRESH-START STEP #1 (recommended): get the loader fibre's TRUE resume PC** (names the real wait directly). The loader fibre's saved SP is at its struct[+0]; on yield, `s3eFibreSwitch_asm` pushed `{r4-r9,sl,fp,ip,lr}` at that SP — the saved **lr** = where the loader is parked. How: extend `marm_dump_fibres` (already reads the fibre table) to, for the NON-current fibre with a non-main stack (loader; main's SP is in 0x7e… range), read struct[0]=SP and dump the pushed-reg block (esp. the saved lr) → disassemble that addr = the exact instruction the loader is waiting at. (NB capture this from the os_thread or while MAIN is current so the loader's SP is actually saved in its struct, not live.)
**STEP #2:** instrument the state machine — log `[r6+0x44]` over time + find what writes it (what transition from state 5 never fires). r6 base = the literal used in 0x1f8e8/0x1f918.
**STEP #3:** it may be a TIMING RACE — freeze frame varied 186→40573 across runs (one run animated 40573 frames then froze). Compare a working vs frozen run to see what diverges.

**TOOLING (all in working tree, env-gated, UNCOMMITTED):** `modules/marmalade.c` diags via env — `APKENV_MARM_FIBREDUMP` (`[FIBRE]`+`[TASK]` dumps), `_STACKSCAN` (`[STACKSCAN]` engine call-chain), `_COOPYIELD`, `_RAMPATCH`, `_GATEBYPASS`, `_TICK`; `compat/pthread_wrappers.{c,h}`+`pthread_mapping.h` = PWATCH blocking-call watchdog (`APKENV_PTHREAD_WATCH`) + pthread_join wrapper. Device launchers: `play-pvz.sh`=CLEAN (all off), `play-pvz-fib.sh`/`-ss.sh`/`-watch.sh`/`-coop.sh`/`-fix.sh`(RAMPATCH)/`-gate.sh`(GATEBYPASS).

**RESUME-COLD CHECKLIST:** (1) device `.88`, legacy-cipher SSH via a `tp.sh`-style wrapper (opts in BUILD-STATE.md). (2) **REBOOT first** → ~500MB free (launches OOM-die below ~440MB; `play-pvz.sh` frees some). (3) re-pull libpvz `/media/internal/.apkenv/lib/armeabi/libpvz.so` (md5 `6b32d855`) to scratchpad; `arm-linux-gnueabi-objdump -d -M force-thumb` (Thumb code; the s3eFibre*_asm fns are ARM → `-M force-arm`). (4) build: `apkenv/build-webos.sh` (two-toolchain); deploy by piping over ssh `cat` (scp's legacy proto fails). (5) deployed binary md5 `959d2288`.

**KEY ADDRS (libpvz base 0x2bf9c000):** main loop `0x1c8e0`; loader-state tick fn `0x1f8e8` (sets state `[r6+0x44]=5`); gate fn `0x1f690` (does per-tick load work — free-RAM/MemRequired check at `0x1f71c` is NOT the blocker); file-task pump `0x1fbf0` (count@`loader_mgr+0x20`, loader_mgr=libpvz+0x717c0); fibre scheduler `0x37008` (`while(fibre.state==1) s3eFibreSwitch`); `s3eFibreSwitch_asm 0x36ec8` (SP@fibre+0, state@+8); fibre table `libpvz+0x74b74` ([+0]=current ptr, 24B structs @ +12..+84); loader fibre stack base ~0x3566d000; deviceYield thunk `0x4196c→0x25a9c`; per-task file-IO proc `0x1f848`.

**❌ GATE-BYPASS TEST = CRASH (PvZ HD, 2026-06-29) — inconclusive; gate does real work.** Added `APKENV_MARM_GATEBYPASS=1` (launcher `/var/apkenv2/play-pvz-gate.sh`): patch the whole loader-state gate fn `0x1f690` entry → `movs r0,#0; bx lr` (always return 0="ready"). Applied cleanly (`[GATEBYPASS] @0x2bfbb690 before=0xb510 0xb082 after=0x2000 0x4770`), but the app **SEGFAULTED at boot in `cfree`/free()** (`signal 11 ... from libc.so.6/cfree`, right after runNative+[MARM-REG]). So forcing 0x1f690→0 skipped its per-tick allocation/state WORK and corrupted the heap → crash. **Conclusion: 0x1f690 does necessary loading work (can't be skipped); the test can't cleanly isolate the freeze (crashes from skipped work).** **NET of both fix attempts: free-RAM check is NOT the blocker (RAMPATCH no-change), and 0x1f690 is on the critical load path but doing real work (GATEBYPASS crash). The actual non-completing condition remains UNIDENTIFIED.** Two fix hypotheses tried, both failed — STOP blind patch-and-test. **TRULY-NEXT (careful, fresh):** (1) get the loader fibre's ACTUAL resume PC — when the loader yields it's saved on its own stack (s3eFibreSwitch pushed regs at fibre SP); read the loader fibre's saved SP (it's NOT current after a clean capture — capture when main is current, then loader struct[0]=saved SP) and disassemble the exact instruction it's parked at = the real wait. (2) Instrument the main-loop state machine: log state var `[r6+0x44]` (set to 5 in 0x1f8e8) over time + what writes it — find what transition never fires. (3) Consider it may be a TIMING RACE (freeze frame varies 186–40573), not a deterministic gate — compare a working run (40573 frames) vs frozen to see what differs. Deployed md5 959d2288; `play-pvz.sh`=clean (all patches env-gated off). RAMPATCH/GATEBYPASS code kept (env-gated).

**❌ FREE-RAM-CHECK FIX REFUTED (PvZ HD, 2026-06-29) — patch applied, NO change.** Implemented `APKENV_MARM_RAMPATCH=1` (`marm_patch_ramcheck` in marmalade.c): runtime 1-byte patch of loaded libpvz, `0x1f72a` `bge.n→b.n` (free-RAM gate always passes), applied before runNative. On-device: `[RAMPATCH] base=0x2bf9c000 patch@0x2bfbb72b before=0xda after=0xe7` (verified applied), but **menu STILL froze (swap=1200), taps still no-op.** So the s3e free-RAM check is **NOT the blocker** — either it was already passing, or `0x1f690` returns "not-ready" via one of its OTHER return-1 sub-checks (0x1f6a2 via 0x3415c, 0x1f6b0 via 0x1c990, 0x1f6cc via 0x1f37c==2, 0x1f70a via 0x3415c) BEFORE the free-RAM compare — patching the free-RAM branch alone changes nothing. **Lesson: the RAM-sensitivity (needs ~500MB) is more likely a TIMING RACE in the fibre handoff** (freeze frame varies 186–40573; a deterministic RAM gate wouldn't race), not this gate. **REFINED MODEL:** loader fibre is post-load IDLE (count=0) just yielding; the main-loop STATE MACHINE (state var `[r6+0x44]`=5, set in 0x1f8e8) stays in the loading state pumping the idle fibre and never transitions to the menu/render state. **BETTER NEXT TEST (cleaner than the free-RAM-branch patch):** force `0x1f690` to ALWAYS return 0 (patch entry `b510 b082`→`2000 4770` = `movs r0,#0; bx lr`) — if STILL stuck, 0x1f8e8/0x1f690 is irrelevant and the spin is purely the fibre pump (0x1fbf0/0x1f7xx/0x1ff30); if it unsticks, the gate IS 0x1f690 (then find which sub-check fails). OR instrument the state machine: log `[r6+0x44]` + what writes it. The post-load fibre/state-machine handoff is the real target. RAMPATCH code stays (env-gated). Deployed md5 576f110f.

**(refuted — kept for the addresses) 🎯 ROOT CAUSE candidate: the menu-load freeze is the s3e startup FREE-RAM CHECK.** Decoded the loader fibre's post-load gate fn **`0x1f690`** (called from the loader-state fn 0x1f8e8, from main loop 0x1c8e0): it reads s3e config keys **`s3e`/`core`/`MemRequired`** (default `0x10000`=64MB; via s3eConfigGetInt 0x2f53c) and **`SkipFreeRamCheck`** (via 0x2f60c), then at 0x1f71c does `free = s3eDeviceGetInt(prop 6)` (=0x33924→0x2578c, a free-MEMORY query) and `if(free < MemRequired) return NOT_READY(1)`. When NOT_READY, the loader fibre never advances past loading → engine keeps pumping it → menu spins forever (the wedge). Config-key strings @libpvz 0x5da5c "MemRequired", 0x5da68 "SkipFreeRamCheck", 0x5da7c "StackSize", + "Out of memory creating %dk stack" → s3e has its OWN memory POOL; the free query almost certainly reports the **s3e pool free (or a conservative value), NOT the TouchPad's actual free RAM** — so it reads < MemRequired even with ~500MB system-free. **THIS EXPLAINS THE ENTIRE SESSION's RAM-sensitivity** (needed fresh ~500MB reboot, froze at random frames, pt5/pt6 "memory" observations). **THE FIX (clean + GENERAL, not a binary patch):** set **`SkipFreeRamCheck`=1** (or lower `MemRequired`) in the s3e ICF config the engine reads (s3eConfig reads via 0x2f53c/0x2f60c from parsed ICF — find the app's `.icf`/config in the apk-assets or add a user-override ICF), OR make the shim's free-memory query (`s3eDeviceGetInt` prop 6) return a large value. **NEXT: locate where PvZ's s3e config/ICF is read** (the JNI `s3eConfigGet`/`s3eConfigGetInt` natives the module registers may be interceptable; else find/patch the ICF) and inject `SkipFreeRamCheck=1`; then the menu should load + be interactive. All addrs: libpvz base 0x2bf9c000, gate fn 0x1f690, MemRequired check 0x1f71c, free-mem query 0x33924, config-get 0x2f53c(int)/0x2f60c, loader-state fn 0x1f8e8, main loop 0x1c8e0.

**(superseded by the FRESH-START PLAN above — kept for addresses) ▶ earlier resume point (PvZ HD, 2026-06-29).** PvZ HD touch WORKS; the menu freeze is a **cooperative s3e-FIBRE stall** (NOT touch/threads/suspend/resources/files — all proven-ruled-out, don't re-chase). Latest: the **loader fibre has FINISHED loading** (task count `[loader_mgr+0x20]`==0) **but stays runnable and yields forever without rendering** (swap frozen). **NEXT TASK:** decode the loader fibre's actual post-load **yield loop** (instruction-level) to find the completion/handoff gate it never satisfies — i.e. why the fibre scheduler `0x37008` (`while(fibre.state==1) s3eFibreSwitch`) keeps re-running the empty loader instead of switching to the render/game fibre. Then implement the missing signal/handoff in `modules/marmalade.c`. **To resume cold:** re-pull libpvz (`/media/internal/.apkenv/lib/armeabi/libpvz.so`, md5 6b32d855) to scratchpad + `arm-linux-gnueabi-objdump -d -M force-thumb`; device `.88` via `tp.sh`-style legacy SSH; **REBOOT device first for ~500MB free** (launches OOM-die below ~440MB); `/var/apkenv2/play-pvz.sh`=normal, `play-pvz-fib.sh`=APKENV_MARM_FIBREDUMP (fibres+tasks), `play-pvz-ss.sh`=stackscan, `play-pvz-watch.sh`=pthread watchdog. **Uncommitted diag code (persists in working tree, env-gated, NOT committed):** `modules/marmalade.c` (`APKENV_MARM_{TICK,COOPYIELD,STACKSCAN,FIBREDUMP}`, `[MARM-REG]`/`[MARM-COOP]`/`[STACKSCAN]`/`[FIBRE]`/`[TASK]` diags + `marm_dump_callchain/fibres/tasks`), `compat/pthread_wrappers.{c,h}`+`pthread_mapping.h` (PWATCH watchdog + join wrapper). Deployed `/var/apkenv2/apkenv` md5 b4ba1a18. Key addrs (libpvz base 0x2bf9c000): fibre scheduler 0x37008, `s3eFibreSwitch_asm` 0x36ec8 (SP@fibre+0, state@+8), fibre table libpvz+0x74b74, loader fibre PC 0x1fb88 / stack 0x3566d000, loader_mgr libpvz+0x717c0 (count@+0x20), main loop 0x1c8e0, deviceYield thunk 0x4196c→0x25a9c. Full layer-by-layer trail below.

**✅ PvZ HD TOUCH CONFIRMED WORKING; real blocker = main-menu LOAD DEADLOCK (2026-06-29).** On-device (clean reboot ~500MB free, `/var/apkenv2/play-pvz.sh`, binary md5 a0e078c8 = HEAD 06e357a): pt7's UI-thread-touch + clean-RAM fixes WORK. Taps reach the engine (16 DOWN+16 UP seen as `[SDLEV] type=5/6`; engine reacts: `getInputString`, `deviceUnYield` 3→194) and the **"tap to continue" screen ADVANCES on tap (user-confirmed)** → so menus DO respond to touch; the open pt7 question is ANSWERED. The remaining symptom ("pre-game/main menu ignores taps") is NOT a touch bug — the **main-menu LOAD deadlocks**, so the engine is frozen, not deaf. Thread signature (device-side watcher `/tmp/watch.sh`, snapshots tid/state/wchan every 2s): main/engine thread (runNative) = **state R BUSY-SPIN on s3eDeviceYield** (deviceYield count →50M+, `glSwapBuffers` FROZEN at ~186), one worker thread = **`futex_wait_queue_me`** (pthread_cond_wait), all others idle (poll/nanosleep). NOT memory (majflt≈17, RSS 383MB, 500MB free); NOT a GPU fence (unlike WMW2 pt4's `kgsl_yamato_waittimestamp` — this is a plain futex). Handoff = a pthread mutex+cond+counter semaphore in libpvz (md5 `6b32d855`, pulled to scratchpad; .text VMA==file-off): **inc@0x26568** (counter@`base+0x148`++ under mutex, no signal) pairs with **dec+`pthread_cond_signal`@0x2653a**; worker waits the cond, never signaled. RACE: one run reached a LIVE animated menu (40573 frames) then a tap-triggered load froze it; another froze at 186 on menu entry. **RE'd THE DEADLOCK (the worker is in the APP-THREAD SUSPEND GATE):** libpvz's single `pthread_cond_wait`@0x23782 sits in the suspend checkpoint fn @0x2374c — `if [r4+0x13c](suspend-requested)!=0 { [r4+0x144]=1; while ([r4+0x148]!=0) cond_wait(cond@r4+0x14c, mutex@r4+0x150) }`. So **`+0x148` = a SUSPEND COUNT; the worker blocks while it's !=0.** `inc`@0x26568 = count++ (suspend), `dec_and_signal`@0x26538 = count--, `pthread_cond_signal` (resume+wake). The engine BRACKETS every JNI call-out with `dec…JNI…inc` (resume around the call, re-suspend after). r4 = a global loader-state struct ptr (loaded from 0x237a4). **Tested fix #1 (tick-on-main) = NO-OP / ruled out:** PvZ's libpvz only exports the native strings `suspendAppThreads`/`resumeAppThreads` — **NOT `runOnOSTickNative`/`runOnOSThreadNative`/`signalResume`/`setResumed`** (grep'd) → `loaderthread.runOnOSTickNative==NULL` for PvZ, so `marm_pump_ostick()` did nothing on either thread. The menu renders purely off the `deviceYield`→`input_update` pump. **So PvZ's ONLY resume lever is `resumeAppThreads`, and the stall-triggered call ISN'T clearing the worker's wait** (count likely >1, or resume clears `+0x13c` but not the `+0x148` the already-waiting thread loops on). NOT a GPU fence (unlike WMW2 pt4). **✅ MECHANISM FULLY CRACKED (2026-06-29 cont) — got the native ADDRESSES by logging `method->fnPtr` at RegisterNatives (`[MARM-REG]` in marmalade.c, libpvz base from /proc/maps).** libpvz base 0x2bf9c000; **suspendAppThreads=file-off 0x2374c, resumeAppThreads=0x23708, runNative=0x23a04, onMotionEvent=0x25c1c.** Decoded all:
- **suspendAppThreads@0x2374c = the SELF-PARK gate**: `if [base+0x13c]!=0 { [base+0x144]=1; while([base+0x148]!=0) pthread_cond_wait(cond@+0x14c, mutex@+0x150) }`. An app thread calls THIS to park itself.
- **resumeAppThreads@0x23708 = NEARLY A NO-OP**: `if [+0x13c]!=0 { lock; [+0x144]=0; unlock }`. It does **NOT** signal the cond or touch the count `+0x148`. **So pt7's stall-triggered resumeAppThreads NEVER COULD wake the worker — that whole approach was built on a false premise.**
- **The real wake = `dec_and_signal`@0x26538** (`[+0x148]--`, `pthread_cond_signal`), paired with **`inc`@0x26568** (`[+0x148]++`). The engine **BRACKETS its JNI call-outs**: `dec_and_signal … <JNI call via 0x243c0> … inc` (3 sites: 0x245f4 etc.) — i.e. count→0 (app threads get a cooperative turn) during the call-out, then count→1.
- **`+0x13c` is NOT a bool** — init fn @0x237e0 sets it to an `s3eFibre*`/thread handle (non-zero) + inits the mutex/cond. So the gate's `if [+0x13c]!=0` ≈ "subsystem up".
- **MODEL: PvZ's app/loader threads are COOPERATIVELY-SCHEDULED real pthreads** — normally parked (count>0), running only during the engine's bracketed JNI call-outs (count→0+signal). **DEADLOCK:** the async menu-load makes the engine SPIN-WAIT on `deviceYield` (an UNbracketed path → no cooperative turn) for the loader thread, which is parked and never gets its turn → wedge. (Worker 7537 parked in suspendAppThreads = NORMAL between turns; the bug is the engine spinning on an unbracketed yield.)
**FIX DIRECTION (general, user chose this over poking):** make `deviceYield` (or a stall) drive the engine's cooperative yield so the loader thread gets turns. Cleanest = call the engine's OWN yield/bracket primitive during `deviceYield` (NOT raw-poke the counter). OPEN: confirm `deviceYield`/`s3eDeviceYield` truly isn't bracketed + identify a clean "run app threads now" primitive vs. replicating `dec_and_signal…inc` (needs libpvz base at runtime — module can parse /proc/self/maps). NOTE: "engine on a bg thread" restructure is BLOCKED by the single GL context (SDL owns it on main; runNative does GL). **❌ COOP-YIELD EXPERIMENT TRIED → FAILED (negative result, refines model).** Implemented `APKENV_MARM_COOPYIELD=1` (`modules/marmalade.c`): from the os_thread, when wedged, replicate the engine bracket — `dec_and_signal(); usleep(2000); inc();` — resolving `dec_and_signal`=base+0x26538, `inc`=base+0x26568, base derived from the resolved `runNative` addr (`runNative&~1 - 0x23a04` = 0x2bf9c000, VERIFIED correct: `[MARM-COOP] enabled base=0x2bf9c000 dec_signal=0x2bfc2539 inc=0x2bfc2569`). On-device (`/var/apkenv2/play-pvz-coop.sh`): coop fired **8000+ times**, but **worker stayed `futex_wait`, swap stayed frozen at 1201, main still spinning** — NO effect. **Why (refined model):** the gate is `while(count != 0) cond_wait` — it releases ONLY at count==EXACTLY 0. If the real suspend-count is **>1** (multiple suspends), one `dec` (1→… not 0) then `inc` just oscillates and never hits 0; and over-decrementing would go NEGATIVE (still !=0, still parked). The engine knows the exact count; we don't → blind dec/inc can't safely hit 0. **OR** worker 10801 isn't on this cond at all (could be a `pthread_mutex_lock`/`sem_wait`/`pthread_join` — all show as `futex_wait_queue_me`; couldn't read the futex uaddr: this kernel leaves `/proc/<tid>/syscall` + `/stack` empty). **🔑 DECISIVE (2026-06-29 final): SUSPEND MECHANISM RULED OUT — worker blocks on a DIFFERENT futex.** Upgraded coop to READ the live loader-state in-process (module computes base from runNative; state struct @0x2c00db28 = libpvz_base+0x71b28, in the .bss anon map 0x2c00c000-0x2c013000; device has NO od/python and busybox dd can't lseek that far, so read in-process not via /proc/mem). At the wedge the log showed: **`count=1` (normal cooperative value, NOT >1), `flag(0x13c)=990240` (the s3eFibre/thread handle, nonzero=enabled), and `stat(0x144)=0`.** `stat` is the "I am parked in suspendAppThreads" flag — set to 1 by the gate when a thread parks. **stat=0 ⇒ NO thread is parked in the suspend gate.** Driving count to exactly 0 (even −1) changed nothing (swap frozen at 250, worker still futex_wait). **CONCLUSION: the app-thread suspend/resume mechanism is functioning normally and is NOT the deadlock** — so ALL suspend/resume work (mine: tick-on-main, coop-yield; AND pt7's resumeAppThreads) targeted the wrong thing. The real blocker = **worker thread (highest tid, the loader) wedged on an UNIDENTIFIED futex (mutex/sem/pthread_join), not the suspend cond**, while the engine main thread spins on `deviceYield`. **TO CRACK IT (needs better tooling — the 2.6.35 kernel leaves `/proc/<tid>/syscall` & `/stack` EMPTY):** get the futex uaddr via gdb-multiarch (not on device; would need cross-gdbserver) OR hook libpvz's pthread imports in apkenv's linker (libpvz imports pthread_* as UND → apkenv resolves them → wrap pthread_mutex_lock/cond_wait/join/sem_wait to log caller+addr) OR inline-trampoline the loader thread fn. Identify what the loader waits on + who should release it. The recurring multi-threaded-load wall is NOT (for PvZ) the suspend gate and NOT (unlike WMW2 pt4) a kgsl GPU fence — it's a plain pthread wait on an unknown object.

**✅ PTHREAD-IMPORT-HOOK INVESTIGATION DONE (2026-06-29) — the wedge is NOT a libpvz pthread lock.** Instrumented apkenv's existing pthread wrappers (`compat/pthread_wrappers.c`): added a lock-free blocking-call WATCHDOG (`APKENV_PTHREAD_WATCH=1`, launcher `/var/apkenv2/play-pvz-watch.sh`) that records `(tid,kind,caller=__builtin_return_address,obj)` on entry to `pthread_mutex_lock`/`pthread_cond_wait`/`pthread_join` (added a `join` wrapper + mapping) and a watchdog thread logs any call still blocking >~2s (`[PWATCH] STUCK ...`). On-device at the wedge (swap FROZEN at 8878, main spinning, worker `futex_wait`): **watchdog logged NOTHING** → the wedged worker's block does NOT go through libpvz's wrapped pthread. **AND `apkenv_my_pthread_create`'s built-in `printf("pthread_create(...)")` logged ZERO calls the entire run → libpvz created NO threads via pthread_create.** So: **(a) the menu LOAD is not on a libpvz worker pthread** (likely s3e FIBRES = cooperative coroutines on the MAIN thread), and **(b) the `futex_wait` worker thread is a DEVICE-LIB thread** (SDL/PDL/Piranha/audio/GPU — created via the device libpthread, bypassing apkenv wrappers; comm="apkenv", likely incidental/idle). Freeze frame count VARIES across runs (186/250/1201/8878) = a RACE, not a fixed gate. **🔑🔑 deviceYield-SPIN CRACKED (2026-06-29): the wedge is a COOPERATIVE s3e-FIBRE stall, not any thread.** Added a stack-scan diag (`APKENV_MARM_STACKSCAN=1`, launcher `/var/apkenv2/play-pvz-ss.sh`): in the `deviceYield` handler, scan the engine thread's CURRENT stack page for return-addresses in libpvz's range → the live call chain as libpvz file-offsets. (Bug fixed: must bound scan to the current 4KB page — the engine runs on a small s3e FIBRE stack; scanning 24KB SIGSEGV'd. Also: `libhgl.so not found` linker line is NON-fatal — plain launch works.) Captured at the wedge (swap FROZEN): **NORMAL chain innermost `0x638b8`; WEDGE chain `0x4196c → …0x2440e 0x2501c… → 0x37008 → 0x1c91c` (×36 identical).** Disassembled (Thumb): **0x37008 is the s3e FIBRE SCHEDULER** — `36ff4: ldrb state,[fibre+8]; cmp #1; 37004: blx s3eFibreSwitch_asm@0x36ec8; 37008: b back` = `while(fibre.state==1) switch_to(fibre)`. The main loop (0x1c8e0; `0x1c918: bl 0x1fbf0`=the yield/pump) spins inside this scheduler. **So: the load is FIBRE-based; the engine endlessly switches to a LOADER FIBRE that stays RUNNABLE (state==1) but never COMPLETES — it yields each pass waiting on an async resource/condition that, in our shim, never becomes ready.** `s3eFibreSwitch_asm`@0x36ec8 = hand-written ARM asm (`stmdb sp!{...}`/`ldmia sp!{...,sp,pc}`) → saves/restores the fibre's SP, so the loader fibre has its OWN stack (separate from main; that's why the main-stack scan shows only the scheduler, not the loader's wait point). **CONCRETE CANDIDATE BUG (investigated → NOT the deadlock):** `modules/marmalade.c:563` `audioPlay` — menu music `music/crazydave.mp3` (offset 737945643 — but apk is only 81MB, and there's NO `music/` dir in the extracted assets, only `sounds/*.wav`) → music is simply ABSENT; audioPlay loads nothing and returns (graceful, called ONCE). **RULED OUT via strace** (`/usr/bin/strace -f` on the wedged process, 4s): at the wedge there are **ZERO `open`/`openat`/`stat`/`access` calls and zero ENOENT** — the loader is NOT hunting any missing file/resource. Syscalls seen = only `write(2)` (our log spam), `nanosleep({0,1000000})` (our os_thread), SDL/PDL event-pipe chatter (`write(30,"x")`/`write(32,"W")` on pipes 95050/95051), and ~508 futex on apkenv-internal locks (0xf7b98 = NOT libpvz). **So the stall is a PURE USERSPACE cooperative-fibre logic wait — no syscall signature (strace can't see it).** The missing music is a separate real bug, not the freeze. **NEXT STEP (defined): inspect the LOADER FIBRE's stack** — hook/trampoline `s3eFibreSwitch_asm` (or find the fibre struct's saved-SP offset) to capture the loader fibre's SP, scan ITS stack for the wait point → identifies the exact resource. THEN provide/complete that resource (general: faithfully service whatever async op the fibre awaits). Stack-scan code in `modules/marmalade.c` (`marm_dump_callchain`, `APKENV_MARM_STACKSCAN`). Deployed md5 a826dede. NOTE: device needs a FRESH reboot (~500MB) to launch reliably; ~420MB it OOM-dies.

**🔑🔑🔑 FIBRE-STACK INSPECTION DONE (2026-06-29) — loader fibre stuck in a TASK-QUEUE loop that never drains.** Added `APKENV_MARM_FIBREDUMP=1` (launcher `/var/apkenv2/play-pvz-fib.sh`, code `marm_dump_fibres` in marmalade.c; /proc/self/maps-bounded so bad ptrs can't fault). `s3eFibreSwitch_asm`@0x36ec8 (ARM) saves SP at `fibre[0]`, state at `fibre[+8]`; fibre TABLE at `libpvz+0x74b74`: `[+0]`=current-fibre ptr, fibre structs (24B stride) at `+12..+84`. Dump at wedge (swap frozen 3247): **current=loader fibre** (struct@0x2c010b98: stale SP, **code/PC at +12 = libpvz+0x1fb88**, own stack base 0x3566d000); the MAIN fibre (struct@0x2c010b80) has saved SP=0x7edb1ae0 (main thread stack), state=0xffffffff. So the loader fibre IS running (it calls `deviceYield` to yield) — that's why `marm_dump_callchain` (current sp) already showed the loader's chain. **The loader's pump = fn @0x1fbf0** (called by main loop `0x1c918`): `r4 = loader_mgr = libpvz+0x717c0` (literal@0x1fefc → 0x717c0); `r5 = [mgr+0x20]` = **PENDING-TASK COUNT**; loops processing the task array @`[mgr+0x24]` (via 0x1f848, callback @`[mgr+0x33c]`) until count==0. **DEADLOCK = the pending-task count `[loader_mgr+0x20]` never reaches 0** — a load task never completes (or keeps re-queuing), so the loader fibre yields forever and the menu never finishes loading. NOT threads, NOT suspend, NOT a missing file (strace), NOT a GPU fence — a cooperative loader-task-queue stall. **NEXT STEP (defined): name the stuck task** — in-process read `[loader_mgr+0x20]`(=libpvz+0x717e0, count) + the task array `[mgr+0x24..]` and log what's pending/looping; OR disassemble the task processor (0x1f848 + the `[mgr+0x33c]` callback). Then make that task complete (likely: faithfully service whatever async op it awaits — a decode/upload/callback the shim stubs). **UPDATE: task processor @0x1f848 is s3e FILE I/O** — its string literals are `"rb"` (open mode), `"Can't open s3e file %s"`, `"Error reading s3e file %s"` (callees 0x3b1c8=s3e open, 0x2dafc/0x1dbb4=read w/ fn-ptr callbacks 0x1f125/0x1f0ed). So the loader's task queue = FILE-LOADING tasks. BUT strace showed ZERO `open()` at the wedge AND no "Can't open" in the log → the s3e files ARE being found/read from the in-memory apk/cache (no syscall), yet the pending count never drains → a task keeps re-queuing OR never reports completion (NOT a missing file). Remaining: log `[loader_mgr+0x20]` (is count stuck at N or growing?) + the current task at `[mgr+0x24]` to see which file/task loops; suspect an async-read-completion or task-callback the shim never signals.

**❌ FILE-TASK-QUEUE THEORY REFUTED (2026-06-29): the queue is EMPTY (count=0).** Added `marm_dump_tasks` (logs `[loader_mgr+0x20]` count, words `mgr+0x20..+0x120`, + filename-string probe on every pointer word; bounds-checked via /proc/self/maps). On-device at the wedge (swap FROZEN at 192, repeated dumps): **`count(+0x20)=0`** consistently → the file-task loop `0x1fbf0` (`if [mgr+0x20]==0 → return fast`) is NOT the spin. (Breadcrumbs: `mgr+0x24=0x2e5a7600` ptr [not a readable string], `mgr+0x28`=`0x653373`="s3e" inline; no other strings/tasks.) **So the loader fibre has FINISHED loading (count=0) but is STILL the current/runnable fibre, yielding forever WITHOUT rendering (swap frozen).** New leading theory: a **post-load completion/handoff gate** — the loader fibre never marks itself done (state stays 1), so the fibre scheduler (`0x37008: while(fibre.state==1) s3eFibreSwitch`) keeps re-running the empty loader instead of switching to the game/render fibre; OR a non-file wait (timer/frame/condition) gates completion. **NEXT: decode the loader fibre's ACTUAL yield loop** — not the task processor. The loader's resume is reached via main-loop `0x1c8e0` → (`0x1f584`/`0x1f950`/`0x1f7xx`→`0x1ff30` run-fibre→scheduler). Find the instruction-level loop that calls deviceYield (the frames near the deviceYield thunk 0x4196c→0x25a9c) and its exit condition; that names the post-load gate. Tooling all in place (`APKENV_MARM_FIBREDUMP` now dumps fibres+tasks). Deployed md5 b4ba1a18. All diag code in marmalade.c (`APKENV_MARM_{STACKSCAN,FIBREDUMP}`), pthread watchdog in pthread_wrappers.c — all UNCOMMITTED + env-gated. Deployed md5 992bfa3b; `play-pvz.sh`=clean. Device needs ~500MB (reboot) to launch reliably.

**(superseded framing) REAL TARGET (next): what the MAIN thread's `s3eDeviceYield` spin is WAITING ON.** It's a fibre/cooperative wait or a device-lib handoff, NOT a libpvz pthread. To find it: disassemble the s3e yield-spin loop's wait condition (find the libpvz caller of the `deviceYield` JNI call — walk the stack / log return addrs in libpvz range 0x2bf9c000-0x2c004000), OR stack-scan the wedged main thread (kstkesp from /proc/<tid>/stat → scan for libpvz return addrs; note device has no od/python/gdb and dd can't lseek far), OR get cross gdbserver. **Code state (all UNCOMMITTED, env-gated, harmless when off):** `compat/pthread_wrappers.{c,h}` + `compat/pthread_mapping.h` (watchdog + join wrapper), `modules/marmalade.c` (`APKENV_MARM_TICK`/`COOPYIELD`/`[MARM-REG]` diags). Deployed `/var/apkenv2/apkenv` md5 40d9f643; `play-pvz.sh`=clean PvZ behavior, `play-pvz-watch.sh`=watchdog, `play-pvz-coop.sh`=coop(ineffective). **DECISIVE NEXT STEPS for a future session:** (1) READ the live suspend state — compute loader-state base (note: apkenv's custom linker maps libpvz's rw/.bss separately; rw seg = file 0x67000→0x2c00b000, so data vaddr V → ~0x2bfa4000+V; the struct global is at file-vaddr 0x71b28; resolve precisely then `/proc/<pid>/mem`-read count@+0x148, flag@+0x13c, status@+0x144) to SEE the real count and whether the worker is even on this cond; (2) if count>1, drive it to exactly 0 (read-modify); (3) if worker isn't on the cond, find what it blocks on (need gdb-multiarch / a hook on suspendAppThreads@0x2374c, or read its futex uaddr via ptrace). **Non-poking alternatives (user prefers):** prevent suspension entirely (keep the Android-foreground state) — needs finding who flips the suspend-enable, OR satisfy the engine's actual wait so it stops spinning on `deviceYield`. The "engine on bg thread" restructure stays BLOCKED by the single GL context. **Code state:** `modules/marmalade.c` UNCOMMITTED experiments: `APKENV_MARM_TICK=main|os|both` (no-op for PvZ), `APKENV_MARM_COOPYIELD=1` (+`APKENV_MARM_{RUN,DEC,INC}_OFF` overrides), `[MARM-REG]`/`[MARM-COOP]` diags. Deployed `/var/apkenv2/apkenv` md5 cb608806; default `play-pvz.sh` runs coop OFF (PvZ-behavior == prior a0e078c8); `play-pvz-coop.sh` runs it ON. None committed (all unverified/experimental). libpvz md5 `6b32d855` in scratchpad + full disasm `libpvz.dis`; watcher `/tmp/watch.sh`. Same multi-threaded-load class as WMW2 (pt4). Device .88 SSH legacy-cipher (`tp.sh` wrapper). Full trail: `plan/STAGE-5-generalize.md` §8.

**◐ STAGE 5 — WMW2 ON-DEVICE: boots, menu interactive, audio, enters level; blocked on level viewport-scale + OOM (2026-06-28).** Ran via a `/var/apkenv2` harness (binary + reused WMW1 libs; apk + extracted assets at `/media/internal/wmw2root`, run with `APKENV_ASSET_ROOT=/media/internal/wmw2root/assets`). Got to `Screen_Main` (the level). **TWO GENERAL bugs found+fixed+committed** (benefit WMW1 + whole toolkit): (1) `GetStringUTFChars` returned the dummy_jstring's own `data` buffer that `ReleaseStringUTFChars` then `free()`s → use-after-free on any string Get/Released >once (WMW2's reused path args → garbage + double-free); fixed to `strdup`. (2) the **asset-root `fopen`/`open` redirect was LOST in the source reconstruction** — restored (on absolute-path read miss, retry under `$APKENV_ASSET_ROOT`); on-demand `/Water/Textures/...` loads now resolve (menu loads via the engine ZipArchive; only the level needed it; **full WMW1 gameplay needs it too**). Also: WMW2 startup args mapped from the deobfuscated `as` storage class (sourceDir/dataDir/cacheDir=writable dirs); `jniRenderInit` floats = physical size in **MM** not dpi. **✅ WMW2 LEVEL-LOAD ROOT CAUSE FOUND (2026-06-28 pt4): MULTI-THREADED GL deadlock.** RE'd to root via on-device thread instrumentation: WMW2's level load is async + worker-threaded (`World::loadLevel(file,Callback)`→loadLevelMetaFile→…Callback→loadLevelPart2→loadLevelDone). Added `[PTHREAD]` lifecycle logging → the loader thread STARTS and never ENDS (hung). Thread `wchan` at the stall: **main = `futex_wait`** (blocked on the loader, frames stop), a **worker = `kgsl_yamato_waittimestamp`** (Adreno GPU fence never resolves), `/dev/kgsl-3d0` open on a non-main thread. **So WMW2 streams/uploads level textures on a BACKGROUND GL THREAD; apkenv has a SINGLE GL context (SDL/PDL, main thread) → worker GL can't proceed → GPU fence hangs → main waits on worker → DEADLOCK.** WMW1 loaded single-threaded so never hit this. NOT memory (412MB free), NOT the present (576x768 fixed). Fix = a major GL-threading project: (1) **marshal worker GL→main thread** (most viable on webOS which forbids raw EGL), (2) shared EGL context (risky, SDL owns it), or (3) force synchronous load if libwalaber has a sync path. CLEAN STOPPING POINT — WMW2 generalization-proven + blocker precisely diagnosed. Diagnostics (`[PTHREAD]`,`[FBOPRES]`) left in. Full trail: `plan/STAGE-5` §8 pt4.

**(superseded) earlier blocker note pt3: the level SCENE does not LOAD on tap — NOT memory.** Fixed the 0.75 shrink: `[FBOPRES]` diagnostic proved the engine renders the level into a **576x768 sub-viewport** of the 768x1024 FBO (bound_fbo=0, bottom-left, clean 3:4 uniform scale); `apkenv_fbo_present` now samples only the engine's last viewport region scaled-to-fill (committed, general; WMW1 = full-FBO unaffected). The physical-mm arg does NOT change 576x768 (engine-internal). **Memory theory RETIRED:** at the stuck state 412MB free, apkenv RSS 52MB, zero readFails/MemWarnings. Real blocker: tap→viewport flips to 576x768 (touch registers) but **no `Screen_*` scene load, no asset loads, black** — and **INCONSISTENT**: the packaged install-dir launch once reached `[Screen_Main] enter`+loading+"ready to start", the /var launch stalls. **Level-load RE plan in `plan/STAGE-5` §8 pt3** (hypotheses: async/threaded scene-load not driven by our single-thread puppeteer [cf WMW1 async widget load]; nav/tap-target; lifecycle/focus gate the packaged path sends but /var doesn't). libwalaber NOT stripped → inline-hook the scene-enter/level-load fns. Tried+committed this session. Toolkit GENERALIZED cleanly (bridge/FMOD/GLES1-FBO/touch all reused). Device left clean, WMW1 install untouched, harness left for fast resume. Full trail: `plan/STAGE-5-generalize.md` §8.

**(earlier) STAGE 5 STARTED — 2nd game (Where's My Water? 2) triaged + RE'd + module written, builds, done offline.** Triaged all 7 candidate apks (table in `plan/STAGE-5-generalize.md` §8): viable natives = wheresmywater2 (Walaber, GLES1, FMOD), bejeweledblitz (SexyApp, **OpenSL**, hidden RegisterNatives), fruitninja (Mortar, GLES2, +ad-JNI +5MB dex); non-fits = cut-the-rope (7MB dex Dalvik), templerun2 (Unity+**Mono** JIT), flappybird (AndEngine all-Java). **Picked WMW2** because it's GLES1+FMOD = the exact subsystems already built → porting it PROVES they're game-independent (the Stage-5 thesis), while still real RE: Disney replaced WMW1's ~14 `WMWRenderer.*` methods with a new 109-export **"GameLib Bridge"** (`com.disney.GameLib.Bridge.*`). **Reused verbatim:** the general FMOD AudioTrack pump (WMW2 ships the same `org.fmod.FMODAudioDevice`), the GLES1 render-to-portrait-FBO, and the **byte-identical Walaber touch interface** (`jniTouch*(I[F[F[I)` etc.). Wrote `apkenv/modules/wheresmywater2.c` (bridge entrypoints + init order); added to build — **both modules compile into ONE binary and self-select via try_init** (each resolves only its own game's symbols = the multi-game-host shape). Builds clean (GLIBC_2.4). Committed locally (NOT pushed — untested public-repo scaffolding, left for user to gate). **[BRINGUP] unknowns for the device:** the 9 `jniWalaberChassisStartup` args (obfuscated `As`/`At` getters), `jniBridgeInit` order, instance-method `thiz` (these natives are instance not static — engine may read fields off it), the `std::string(NULL)` boot bug (`_S_construct null not valid` present in libwalaber — bake the 1-insn patch if it crashes), portrait confirm, dpi tune, and whether in-level touch needs WMW1's frame-pacing/thief handling. Full trail: `plan/STAGE-5-generalize.md` §8.

**✅ PACKAGING DONE (Stage 6) — WMW launches from the TouchPad LAUNCHER ICON as a .ipk (2026-06-28).** `.ipk` id `com.apkenv.wheresmywater` installs via `palm-install` over novacom and launches from the launcher; jailed app boots, GL on Adreno 220, FMOD audio (rate=24000), smooth render loop. Build: `scp` device bionic libs (`/var/apkenv/libs/webos`) → project `apkenv/packaging/staging-libs`, then `./packaging/build-ipk.sh` → `packaging/out/*.ipk`. Reinstalling needs an `appinfo.json` version bump (palm-install refuses same/lower). **TouchPad PDK launch path (HARD-WON — none of this is in the webOS-MCP `pdk`/`gotchas` docs; found by catching the live pid):** LunaSysMgr runs PDK apps in a **hybrid jail** (`/var/palm/jail/<appid>/`) as **uid 5003 (jailuser)**, `LD_PRELOAD=libpvrtc.so`, CWD/HOME=app dir. **`/media/internal` is bind-mounted RW into the jail** (real game data visible there → only the binary needs the exec app partition; the apk's native libs load via apkenv's own anon-RWX userspace linker, fine on noexec media). Gotchas, all handled in `apkenv.c` `#ifdef __webos__` (committed): (a) sysmgr passes the **launch-params JSON as argv** (`apkenv "{ }"`) → packaged launch must force `APKENV_DEFAULT_APK` (else "Not a native APK" → exit); (b) **`main` must be the native ARM binary** — a shell-script main is NOT exec'd; (c) a jailed PDK app's **stdout/stderr go NOWHERE** (not even /var/log/messages) → redirect to `/media/internal/apkenv-wmw.log` from inside the binary; (d) **`argv[0]` from the launcher is unreliable** → self-locate via `/proc/self/exe`, chdir to app dir so `./libs/webos` resolves; (e) the jail is **torn down on exit** AND **can't be ptraced even as root** → read the in-jail log LIVE via `/proc/<pid>/root/media/internal/...`. webOS-MCP knowledge (read this session, user-steered): `webos://knowledge/{pdk,gotchas,app-structure}` (confirms jail, SDL-owns-GL/no-EGL, /media/internal writable). **SELF-CONTAINED .ipk (2026-06-28):** the .ipk now BUNDLES the game apk — fresh device needs nothing pre-staged. Verified: removed all cruft (`/var/apkenv`, `/media/internal/gameroot`, the loose `/media/internal/wheresmywater.apk`), installed v1.0.1, launched from the launcher → boots from the BUNDLED `android/wheresmywater.apk` with GL+audio, nothing on /media/internal but the savegame dir. App-dir layout organized for re-packers: `apkenv`/`appinfo.json`/`icon.png`/`README` + `libs/webos/` (webOS host libs) + `android/<game>.apk` (the Android bits; packaged launch auto-finds the first *.apk there). Re-pack a different game = drop its apk in android/ + add a matching `modules/<game>.c`. Build inputs: `packaging/staging-libs` (device bionic libs) + `packaging/wheresmywater.apk` (the patched apk; `$APK` overridable). Device IP .88 reachable via SSH again; novacom-USB also works. Stages 4 + 6 BOTH green on device; WMW is a one-tap installable webOS app with sound. Bundling the apk is OK only for FREE/ARCHIVAL games; for paid/proprietary content the apk must stay EXTERNAL (user supplies their own = emulator/BYO-ROM model) — that future distribution path is DESIGNED (not built) in `plan/STAGE-6-packaging.md` §8: apk-discovery precedence list (config → user drop dir on /media/internal → bundled → default), missing-apk SDL prompt, module `match()` validation, and runtime-patching the extracted .so (never ship/mutate game binaries). Full trail: `apkenv/BUILD-STATE.md` + `plan/STAGE-6-packaging.md` §7.

**✅ AUDIO WORKS ON DEVICE (Stage 4 DONE) — user-confirmed audible (2026-06-28).** WMW now runs end-to-end on the TouchPad WITH SOUND. On-device: FMOD mix rate read dynamically = **24000 Hz stereo** (not 44100); SDL 1.2 audio backend opened fine (`ring=32768 sdlframes=512`), DSP `dspLen=512 dspNum=4 chunk=2048`, mixer stable (single `[FMOD] init`, no errors, no rebuild). New known-good binary md5 `130fd02b` deployed `/var/apkenv/apkenv` (pre-audio playable build saved `/var/apkenv/apkenv.prev`; revert = `cp apkenv.prev apkenv`). Device reachable via SSH .88 again (first "no route" was transient) AND via novacom-USB (`novacom run file:///bin/sh ...`). Implementation detail below:

**✅ AUDIO IMPLEMENTED (Stage 4) — host-validated, then VERIFIED ON DEVICE (2026-06-28).** WMW's FMOD path is confirmed **AudioTrack** (not OpenSL): `libfmodex.so` has `fmod_output_audiotrack.cpp` + exports auto-bound JNI `Java_org_fmod_FMODAudioDevice_{fmodGetInfo,fmodProcess}`. The device is created by the GAME's Java (`WMWActivity`→`new FMODAudioDevice();start()` in onCreate/onResume, `stop()` in onPause) — NOT by libfmodex — so the trigger = our existing `onGameResume`/`onGamePause` module hooks. Decompiled `FMODAudioDevice.run()` (stereo S16 MODE_STREAM; `fmodGetInfo` selectors 0=rate 1=dspLen(frames) 2=dspNum 3=mixerRunning; chunk=`allocateDirect(dspLen*2*2)`; loop `if getInfo(3)==1: fmodProcess(chunk); track.write(full)`) and reimplemented it in C since no Dalvik runs that thread. `fmodProcess` disasm: calls exactly ONE JNIEnv fn — `GetDirectBufferAddress` (table byte 920) — for the raw PCM ptr; ignores thiz/capacity. **Built (all GENERAL, no WMW audio constants), committed in project `apkenv/` git:** `audio/audiotrack.{c,h}` (S16 streaming sink = lock-free SPSC ring drained by SDL pull callback; consumer never blocks + zero-fills underrun, producer blocks-when-full = real-time pacing, bounded so a paused sink can't wedge join), `audio/fmod_pump.{c,h}` (C port of run(), resolves the JNI exports via `lookup_symbol`, pumps on its own thread), `jni/jnienv.c` (implemented `NewDirectByteBuffer`/`GetDirectBufferAddress`/`Capacity` — were stubs), `platform/common/sdl_audio_impl.h` (**NULL `obtained`** so SDL converts to desired format — else wrong-pitch on hw), `modules/wheresmywater.c` (pump start/stop on lifecycle; `APKENV_WMW_AUDIO=0` disables). **Validated OFFLINE (device unavailable):** 8M-sample producer/consumer ring stress test (real audiotrack.c, stubbed SDL) → no corruption, no loss, underruns zero-filled; **ThreadSanitizer CLEAN**. Adversarial review done + fixes applied (NULL obtained, dsp_len/calloc guards, freed leaked fake ByteBuffer, dropped a two-writer tail race, clamped round_pow2). **NEXT (needs device): redeploy `./apkenv` (on-device `/var/apkenv/apkenv` is STALE/pre-audio), run play.sh, LISTEN** — menu music + carve/UI SFX audible & in sync, no dropouts, clean pause/resume. Watch `[FMOD]`/`[AudioTrack]` log lines. First risk: webOS SDL 1.2 audio backend opening + honoring FMOD's mix rate (24000 vs 44100 — pump reads it, not hardcoded). **Packaging (Stage 6) SCAFFOLDED:** `apkenv/packaging/{webos/appinfo.json,build-ipk.sh}` produces a valid runtime-only .ipk via palm-package (game apk stays on /media/internal; `APKENV_DEFAULT_APK` lets the launcher-launched binary find it). BLOCKED on device for: bionic-libs payload (device-only → `packaging/staging-libs`), CWD/exec verification, and the **asset-root fopen redirect** which (note!) is NOT in this reconstructed source tree (`my_fopen` is a passthrough) — resolve before a no-env launch. Full trail: `apkenv/BUILD-STATE.md` + `plan/STAGE-4-audio.md` §8 + `plan/STAGE-6-packaging.md` §7.

**⚠️ CORRECTION (2026-06-27 — supersedes every "finger is never caught / FingerCatcher never catches our finger / force-catch needed" conclusion below).** Static RE of `WidgetManager::update` (0x1ece38) + an on-device `[WMWUPD]` probe proved the in-level finger **IS captured** — by a `Walaber::Widget_PushButton` (runtime 0x5afee8, vtable+8=0x469928), **not** the carve `Widget_FingerCatcher` (0x565ff8, vtable+8=0x469618, whose count stays 0). Both AABBs contain the finger; `update()`'s "Phase 2" dispatches `acceptNewFingerDown` in entered-tree (widget-id) order, the PushButton sorts first, captures, and the loop exits → FingerCatcher starved. So the carve bug is **widget-capture ARBITRATION**, not no-capture; force-catch was masking it. DISPROVEN as causes (don't re-test): coordinates, per-frame dt (update's dt arg is unused), touchMoved-catch (touchMoved never captures, only updates position), delivery/threading/cadence. Likely real cause: a full-screen/overlay PushButton left present+enabled during play by our incomplete Activity/lifecycle init. Full trail: project `plan/STAGE-0-harness.md` §8 + [[android-runtime-plan]].

**UPDATE (2026-06-27) — cadence FIXED, arbitration is the remaining blocker.** Two distinct sub-bugs: (1) **CADENCE** — a tap's DOWN+UP were landing on the SAME engine frame (proven: `[WMWTOUCH] f1723 BEGAN+ENDED`), so the per-frame `WidgetManager::update` Phase-2 capture never ran between them and the finger was ABSENT from the map at update time. FIXED by **frame-paced delivery** (`APKENV_WMW_PACE`, default-on, general): dispatch ≤1 transition/finger/engine-frame (DOWN frame N, UP held to a later frame). On-device: finger now PRESENT + captured. KEEP. (2) **ARBITRATION** (still open) — the captured widget is the full-screen **"press Swampy" `Widget_PushButton`** (vtable+8=0x469928; `handleEvent a=10`→`SwampyPressOnLevel`), NOT the carve `Widget_FingerCatcher`. It wins because the Phase-2 entered-tree dispatches `acceptNewFingerDown` in `widget+0x10` order and the button sorts ahead of the carve; `PushButton::releaseFingerMoved` returns 0 so a drag on it is silently eaten. On Android the carve works ⇒ FingerCatcher must sort first there ⇒ difference is **level-build widget state/order** (lifecycle / async-load sequencing, Stage 1), not delivery. force-catch still beats it. Full trail: `plan/STAGE-2-input.md` §8.

**✅ IN-GAME TOUCH SOLVED — root cause = a full-screen "press Swampy" `Widget_PushButton` (2026-06-27).** Traced fully: in-level the finger is captured by a full-screen (AABB `0,0..768,1024`), key-0 `Widget_PushButton` (vt+8=0x469928, fires `SwampyPress`/`handleEvent a=10`) that swallows EVERY touch on EVERY level (carve `FingerCatcher` key=5 + HUD buttons all starve — it grabs the single key-0 entered-tree slot; keys are content-fixed via `addWidget(w,key)` `str r6,[r4,#16]`). Confirmed on level 2 (no tutorial) too. **KILL-THIEF experiment (`APKENV_WMW_KILLTHIEF=1`, `/var/apkenv/play-kill.sh`): disable that full-screen PushButton (`+0x60=0`) + release the finger → carve AND HUD both work on-device.** So everything else (cadence fix, capture machinery) is correct; this one widget was the sole blocker. **WMW now PLAYABLE end-to-end on webOS: FBO water + frame-paced touch (`APKENV_WMW_PACE`, default-on) + thief neutralized.** Also fixed this session: in-level touch CADENCE — taps' DOWN+UP were collapsing into one engine frame so the per-frame capture never ran between them; fixed by frame-paced delivery (≤1 transition/finger/frame). REMAINING (refinement, not blocking): make the thief fix precise — why its bounds are full-screen vs localized-on-Android, why `releaseFingerMoved` (returns 0) doesn't hand drags to the carve; current kill is heuristic (vtable + full-screen check) and disables tap-Swampy. The disproven theories (coords/dt/threading/load-order) stand. Full trail: `plan/STAGE-2-input.md` §8.

**✅ WATER ORIENTATION BUG FIXED (2026-06-27 — supersedes the "WATER/FLUID ORIENTATION BUG ... NOT fixed" note below).** Fixed by the **render-to-portrait-FBO** architecture (Stage 3): the engine renders its whole frame into an offscreen 768×1024 portrait FBO (apkenv redirects its fb-0 binds there), and the platform presents it to the native 1024×768 FB as ONE 90°-rotated textured-quad blit — retiring the per-call glOrthof/viewport/scissor rotation hooks. Self-consistent portrait frame ⇒ world, UI AND fluid sim all agree. Confirmed on-device: full-screen upright, water settles to portrait-bottom. Two bring-up bugs (both real runtime issues, both fixed): present() must save/restore the viewport (engine sets it once at init) AND all GL state it touches (else the attract screen's between-frame async skeleton load corrupts and the intro hangs on `Screen_Credits`). Env-gated `APKENV_WMW_FBO=1` (`/var/apkenv/play-fbo.sh`); make default + delete legacy hooks once in-level touch is solved. Code: `apkenv.h` ModuleHacks.{render_to_fbo,fbo_w,fbo_h}, `compat/gles_wrappers.c` (apkenv_fbo_present), `platform/webos.c`, `modules/wheresmywater.c`. Full trail: project `plan/STAGE-3-orientation-sensor.md` §8.

Progress on the slim NDK→webOS game wrapper (see [[acl-anatomy]], [[android-apk-port-triage]], [[templerun2-port-analysis]]). Workspace: scratchpad `spike/` (bionic/ = harvested ACL linker+libs; games/ = libwmw+libfmodex; probe/).

**SOURCE LOCATION (PERSISTENT — 2026-06-27):** The apkenv port source now lives in the PROJECT at `/home/jonwise/Projects/webos-android/apkenv/` (git-tracked), NOT in scratchpad. It was LOST once (prior session kept it only in the ephemeral scratchpad, which is wiped at session end) and had to be reconstructed from upstream `thp/apkenv` + these notes. Full rebuild recipe + state in the project file `apkenv/BUILD-STATE.md`. Reconstructed files: platform/webos.c, modules/wheresmywater.c, apklib/apklib.c (zip-dir fix), compat/pdk_compat.h, glshim/ (Khronos headers), devlibs/libEGL.so, build-webos.sh. Rebuilt binary boots clean on device (md5 b33284b9..., 397KB, GLIBC_2.4, interp ld-linux.so.3). NEVER keep this source only in scratch again. ACL itself = no source either (RE'd, Gingerbread AOSP); RE notes ref: github.com/webOSArchive/acl. Project goal: run Android games WITHOUT ACL, wrapping in only what each game needs.

**DECISIVE 2026-06-27: in-level touch IS delivered to the engine; the level scene ignores it (NOT a delivery/SDL/PDL/coords/multitouch problem).** Added SDL-event + per-frame heartbeat instrumentation to platform/webos.c ([SDLEV]/[SDLHB]) and dispatch logging to the module ([WMWTOUCH]). Controlled on-device test: in Screen_WaterTest (first_dig.png), with the game fully animating + tutorial showing, the user swiped and **771 well-formed [WMWTOUCH] dispatches reached rendererTouchMoved** — a clean BEGAN→MOVED stream, smooth drag, normalized coords dead-center of play area (~0.49x, 0.17→0.46y) — and the engine logged ZERO response (no swampy/carve/dig/fluid/Level/MiscEvent). Frames kept advancing (heartbeat climbed), so NOT the SDL_ACTIVEEVENT busy-wait (that path only fired on the user's deliberate task-switch: ACTIVEEVENT state=0x4 gain=0 → pause→resume, which is what pops Screen_MenuOverlay/pause menu — working as intended). So menus (Screen_MainMenu/WorldSelect/LevelSelect) consume the SAME rendererTouch* path fine, but Screen_WaterTest does not. User initially believed "no touch lines appear in-game" (inferred from screen behavior, no live log) — instrumentation disproves that part, but CONFIRMS their core "it's not coordinates / nothing happens anywhere" (engine isn't carving in the wrong place, it's not carving at all). **CONCLUSION: bug is INSIDE libwmw's level-scene input dispatch.** Leading non-coords hypothesis (from §6c): the UI-thread(onTouchEvent enqueue)/GL-thread(onDrawFrame drain) handoff — our single-thread shim calls rendererTouch* and rendererDrawFrame on ONE thread; the level sim may gate touch processing on a thread-identity or cross-thread queue signal that menus don't. NEXT FORK: (a) instrument libwmw internally (bake log trampolines at the touch receiver-check @file-off 0x10f51c + level scene dispatch) — definitive but heavy RE; OR (b) two-thread experiment — dispatch rendererTouch* from a dedicated "UI thread", keep rendererDrawFrame on main "GL thread"; if the level then carves, it was the threading handoff.

**📋 CONSOLIDATED HANDOFF (2026-06-27): see project file `apkenv/HANDOFF-touch-water.md`** — full self-contained record of BOTH the touch-capture and water/rotation issues: verified pipeline, root causes w/ addresses, the WMW-specific force-catch stopgap, everything TRIED & FAILED (multitouch, thread-split, double-down, enter-catch, REHIT/REHIT2, orientation sweep, gravity feed, FBO-aware rotation), leading hypotheses, the architectural takeaway (render game to portrait FBO + rotate once at presentation), and the device env knobs/launchers. Read that doc first when resuming.

**WATER/FLUID ORIENTATION BUG — localized (2026-06-27, NOT fixed).** World + UI + menus render UPRIGHT correctly (always did). Only the WATER is wrong: the fluid SIMULATION runs in a 90°-rotated (LANDSCAPE) frame — water settles/floats where the "landscape ditch" would be, not the rendered (portrait) ditch. Carve works because carve + fluid grid share that rotated frame; the visual terrain is the other frame. INDEPENDENT of camera/gravity: changing GameSettings::CurrentDeviceOrientation (APKENV_WMW_ORIENT, set BEFORE rendererInit), feeding the accelerometer (APKENV_WMW_ACCEL/GX/GY), and the FBO-aware rotation fix ALL left the water unchanged. So it's the fluid grid/terrain-bitmap orientation, not the world transform. Fluid render = Walaber::FluidParticleSet::drawParticles (via SpriteBatch/camera); grid = WaterConcept::Fluids::_initFluidsWithBounds(AABB) -> Walaber::Grid::Grid(origin,cellW,cellH,cols,rows). NEXT: RE the fluid grid bounds + terrain-collision-bitmap orientation (why landscape); suspect bounds/grid derive from the real landscape surface (1024x768) or sRealScreenSize(was 0,0) rather than the portrait logical size. DEEP — separate focused session. (User confirmed Android playthrough is fine, so it's OUR bug, not game-inherent.)

**APKENV GENERAL FIX (2026-06-27): FBO-aware screen-rotation hooks.** compat/gles_wrappers.c: added `int apkenv_bound_fbo` (set by my_glBindFramebufferOES + my_gles2_glBindFramebuffer); the glOrthof rotation, glViewport swap, and glScissor swap now apply ONLY when fb 0 (real screen) is bound, never while rendering into an offscreen FBO. Correct for render-to-texture games generally. (Did NOT fix WMW water — that's the fluid-frame issue above — but it's the right behavior.)

**ORIENTATION (2026-06-27):** GameSettings::CurrentDeviceOrientation (link 0x46f97c) controls the gravity-sign negate in Screen_WaterTest::accelerometerChanged (negates when ==1) and feeds the orientation logic. Set it BEFORE rendererInit (module sets it at init via APKENV_WMW_ORIENT) — a per-frame write is too late (camera caches). World was already upright regardless; orient mainly matters for gravity. Accelerometer was NEVER fed before; module now feeds accelerometerChanged(gx,gy,0) each frame (default 0,-9.81; env APKENV_WMW_GX/GY).

**WRAPPER PHYSICS/WATER BUGS (2026-06-27, user A/B'd vs real Android): OUR webOS wrapper has physics/water problems that the Android playthrough does NOT.** So these ARE wrapper bugs (not game-inherent). Likely a GENERAL runtime-level cause that could ALSO underlie the touch-capture failure — watch for a common root: (a) wrong per-frame delta-time the engine computes (rendererDrawFrame takes no dt; engine derives it from a clock — wrong dt => unstable fluid sim AND time-gated input logic failing), (b) FP environment (FPSCR / NEON denormal-flush / rounding mode differing from Android => accumulating sim error; note discrete FP compares like AABB::contains work, so it'd be precision/accumulation not gross breakage), (c) clock source apkenv exposes (gettimeofday/clock_gettime/SystemClock). NOT the focus now (touch first) but keep the common-cause lens.

**STRATEGY (2026-06-27, user steer): touch fix must be GENERAL (works for many NDK games), not WMW-specific.** The force-catch (calling WMW's Widget_FingerCatcher::_acceptFinger directly) is a STOPGAP that makes the carve work but is game-specific (hardcoded vtables/offsets). The real fix belongs in the apkenv input/runtime layer: our touch DELIVERY is faithful (matches WMWView.copyTouches), so the gap is the RUNTIME model (MotionEvent action/pointer semantics, per-frame event cadence, or UI/GL thread + engine mutex handoff) that stops the engine's OWN finger->widget capture from firing. Plan: (1) fully map why the engine's catch (WidgetManager::update enter-detection) skips our finger — instrument FingerInfo state [WMWFI] + whether acceptNewFingerEntered ever fires [WMWENTER]; (2) map to a general wrapper behavior; (3) fix/rewrite in apkenv so it generalizes. User OK with rewriting a library to do it right.

**✅ IN-GAME TOUCH (carve) WORKS via force-catch STOPGAP — WMW gameplay PLAYABLE on webOS (2026-06-27). Finished a level, 3 stars.** Fix = FORCE-CATCH: the slim shim's single touchDown always takes WidgetManager::touchDown's CREATE path (never the hit-test/catch path), and the per-frame enter-detection never catches our finger either, so Walaber::Widget_FingerCatcher reported 0 caught touches (carve dead). Solution (in modules/wheresmywater.c, env APKENV_WMW_FORCECATCH, device launcher /var/apkenv/play-catch.sh): each frame in Screen_WaterTest, if finger id 0 is in the WidgetManager(screen+0x50) finger-map and not yet caught, CALL the engine's own Widget_FingerCatcher::_acceptFinger(catcher,0,FingerInfo) directly (catcher ptr captured from Screen_WaterTest::handleEvent's widget arg; _acceptFinger@link 0x1fe428+slide; finger-map walk = std::map<int,FingerInfo*> at wm+0x28, node key@16 value@20). Result: World::handleTouchDown fires (921x/level), handleEvent count[+12]=1, LevelEvent EventType:Win Stars:3. NOTE: HUD pause/reset BUTTONS still may not capture (separate widgets, not the FingerCatcher) — minor, level is completable via carve. TODO: make force-catch the DEFAULT (currently env-gated) + strip debug hooks for a clean build; then sound (FMOD) + .ipk packaging. Full detail: project apkenv/BUILD-STATE.md.

**(localized, now FIXED) ROOT CAUSE — in-game touch (2026-06-27). Carve widget never "catches" our finger.** Pipeline all verified correct: finger->rendererTouch*->pumpEvents (every frame, rendererDrawFrame calls it)->ApplicationContext::touch*->ScreenManager::touchMoved (gate mCurrentTransition=nil=OPEN)->active screen IS Screen_WaterTest->Screen_WaterTest::touch* (inline-HOOKED, FIRE in-level)->WidgetManager::touch* (this+0x50, manager gate +0x60=1). Real coords SANE (768x1024 portrait); button taps HIT correct button AABBs (valid=1). The gameplay widget = **Walaber::Widget_FingerCatcher** (full-screen inf-bounds). It fires Screen_WaterTest::handleEvent(a=5) every frame but with WidgetActionRet+12 (touch count)=0 => handleEvent bails (no carve). Count = FingerCatcher[+0xbc] = 0 because the catcher NEVER catches our finger: _acceptFinger fires 0x; World::handleTouch{Down,Moved,Up} fire 0x. Catch requires acceptNewFingerDown (from WidgetManager::touchDown HIT-TEST path 0x1ede08) OR acceptNewFingerEntered (gated catcher+0xe0, from WidgetManager::update enter-detection). NEITHER fires: (1) touchDown for a FRESH finger id takes the CREATE path (alloc FingerInfo, return) and provably never branches to the hit-test (0x1ede08); hit-test only runs if the finger id ALREADY in the map (wm+0x28) with FingerInfo+20==0. We send 1 BEGAN/finger=>always fresh=>never hit-test. (2) enter-detection never sees an outside->inside transition (finger born inside inf-bounds catcher); setting catcher+0xe0=1 didn't help. EXPERIMENTS ALL NEGATIVE (device launchers /var/apkenv/play-{mt,regain,force,dd,ec}.sh; envs APKENV_WMW_{MULTITOUCH,UITHREAD,CALLREGAIN,FORCEINPUT,DOUBLEDOWN,ENTERCATCH}): multitouch, UI/GL thread split, call regainedTop, force manager gate, double-BEGAN (engine likely dedups 2nd down), enter-catch. PARADOX to crack next: how does Android capture on a fresh down if create-path never hit-tests? -> RE-READ decompiled WMWView.copyTouches (baksmali classes.dex) + verify ndk MotionEvent action codes (rendererTouchBegan vs pumpEvents 0=began/1=moved/2=ended); OR bake a libwmw patch making touchDown's create path also hit-test (capture on fresh down). All key offsets in BUILD-STATE.md. Inline-hook harness persists in modules/wheresmywater.c.

**(earlier) ROOT-CAUSE HUNT — narrowed to WidgetManager hit-test / coordinate-space or per-widget enables (2026-06-27). Manager gate is NOT it.** Screen_WaterTest::touchMoved/touchDown forward to this->WidgetManager(this+0x50) -> WidgetManager::touchMoved(0x1ea99c)/touchDown(0x1edc10), both gate on byte WidgetManager+0x60. BUT WidgetManager ctor inits +0x60=1 (mov r2,#1; strb) and nothing writes 0 to the *manager's* own +0x60 (regainedTop/_finalLoadStep write sub-widgets via getWidget(id)+0x60), so the master gate is ~certainly 1 in-level => not the cause. regainedTop() enables sub-widgets getWidget(7/8)+0x60=1 — that's why task-switch (pops pause -> regainedTop) restores touch. _finalLoadStep called from async cbs _finishedLoadingWidgets/_loadLevelPart2 (fired, level loads). Carve = World::handleTouch* via indirect/registered receiver. NEXT: REAL runtime coords needed (Walaber::Vector2 passed BY POINTER; fixed hook now derefs + logs wm gate). Leading hypotheses: (A) coordinate-SPACE mismatch in WidgetManager hit-test (AABB::contains) — level widgets/carve in world/camera or portrait 768x1024 space vs our normalized/rotated coords; menus work because menu widgets match our coord space. Re-examine coords despite prior "not coords" (degenerate mapping also = nothing anywhere). (B) per-widget +0x60 enables. Deployed build has fixed hook + [WMWSTACK] gate log + APKENV_WMW_FORCEINPUT (play-force.sh) control test. TEST WHEN USER BACK: play.sh, enter level, swipe -> read [WMWHOOK]/[WMWSTACK] for real coords + gate.

**(earlier) ROOT-CAUSE HUNT NARROWED (2026-06-27, via libwmw static RE + runtime probes). Touch dispatch reaches the gameplay screen handler — bug is BELOW it.** Findings, all confirmed: (1) Touch gate `Walaber::ScreenManager::mCurrentTransition` (GOT-resolved global @link 0x470ec0; touchMoved drops touches while it's non-NULL) is OPEN (nil) in-level — NOT the cause. (2) rendererTouchMoved(0x10e258) enqueues an `ndk::MotionEvent` (mutex + list hook); `rendererDrawFrame`(0x10deac) calls `pumpEvents()`(0x10d8b0) every frame which drains the queue → `ndk::ApplicationContext::touchMoved`(0x1045e4) → `Walaber::ScreenManager::touchMoved`(0x274104). (3) ScreenManager::touchMoved iterates `mScreenStack`(@link 0x470e78); runtime probe shows in-level screens=1, top=Screen_WaterTest (vtable ptr link 0x46b4b8 = _ZTV..Screen_WaterTest+8). So WaterTest IS the sole active screen. (4) Screen_WaterTest vtable: +0x28 touchDown, **+0x2c touchMoved (the handler the dispatcher calls)**, +0x30 touchUp, +0x40 GameScreen::consumesInput (return decides stop/continue — I initially mis-read +0x40 as the touch fn). (5) **Inline-hooked Screen_WaterTest::touchDown(0x3aea8c)+touchMoved(0x3aec0c) — THEY FIRE in-level (14 down, 22 moved).** So the handler executes. Screen_WaterTest::touchMoved just forwards to `this->WidgetManager`@obj+0x50 -> `WidgetManager::touchMoved`(0x1ea99c) [HUD]; carve is `WaterConcept::World::handleTouch*`(0x2c3c38 etc.) called INDIRECTLY (registered receiver, no direct bl). Both HUD+carve dead though handler runs => **bug is in WidgetManager dispatch and/or the World receiver wiring, downstream of the screen handler.** NOTE: Walaber::Vector2 passed BY POINTER (hidden ref, non-trivial type) in r2/r3 — my first hook misread it as by-value -> garbage coords (hook bug, not engine). NEXT (offline-capable): fix hook to deref Vector2; disasm WidgetManager::touchDown(0x1edc10)/touchMoved(0x1ea99c) for widget-list iteration/gating; find World receiver registration (Screen_WaterTest::enter/_buildUI). Tooling: libwmw NOT stripped (7569 syms) at scratchpad wmwlib + saved log apkenv/last-wmw.log. Inline-hook harness now in modules/wheresmywater.c (wmw_install_hook, env APKENV_WMW_HOOK).

**THREADING-HANDOFF THEORY DISPROVED (2026-06-27).** Added a real "UI thread" in modules/wheresmywater.c (env APKENV_WMW_UITHREAD, default on): main thread polls SDL + runs rendererDrawFrame (the "GL thread", tid logged), a dedicated pthread drains a touch queue and calls rendererTouch* (the "UI thread", distinct tid logged) — mimicking Android onTouchEvent/onDrawFrame thread split. On-device: 837 in-level [WMWTOUCH] dispatches from the UI thread, swipes well-formed, engine STILL zero response. So the single-thread handoff was NOT the cause. Eliminated now (empirically): SDL/PDL delivery, coords, multitouch, thread-handoff. **libwmw is NOT stripped — 7569 C++ symbols.** Touch dispatch chain (demangled): rendererTouchMoved(JNI) -> Walaber::ScreenManager::touchMoved(int,Vector2,Vector2) -> active Walaber::GameScreen::touchMoved -> WaterConcept::Screen_WaterTest::touchMoved (gameplay override @ libwmw file-off 0x3aec0c; touchDown @0x3aea8c, touchUp @0x3aeabc, enter @0x3b13d4, update @0x3beba8, _buildUI @0x3af100). Menus use same ScreenManager->Screen->WidgetManager path and WORK. NEXT: static-disassemble ScreenManager::touchMoved (routing decision) + Screen_WaterTest::touchMoved (does it early-return?) + Screen_WaterTest::enter (does it register as touch responder?) to find why the level screen doesn't act. libwmw extracted at scratchpad wmwlib/lib/armeabi-v7a/libwmw.so; symbols make static RE tractable (objdump + demangle).

**MULTI-TOUCH SWIPE WORK (2026-06-27, theory disproved — single-finger n=1 throughout; not the issue):** Rebuilt module has a runtime touch-mode switch. `APKENV_WMW_MULTITOUCH=1` (device: `sh /var/apkenv/play-mt.sh`) = real per-finger aggregation (Began/Moved/Ended dispatch ALL active fingers w/ real ids) for in-game swipe/carve. Default/unset (`play.sh`) = LEGACY single pinned finger id 0 count=1 (the menu-working path). Both log `[WMWTOUCH] f<frame> BEGAN/MOVED/ENDED mode=.. n=.. {id,x,y}` to /tmp/wmw.log — correlate with engine's `ScreenEnter:Screen_WaterTest` analytics. Hypothesis (user-supplied): in-game is swipe-heavy and needs multi-touch enabled. Still to verify on-device.

**OPEN ITEM — active-gameplay touch (2026-06-26):** UI/menu touch fully works (navigate, launch levels), but the active gameplay scene **`Screen_WaterTest`** (carve-by-drag + in-game HUD/pause) does NOT respond. Confirmed (normal run, /tmp/wmw.log): in-game taps reach the engine well-formed (0 finger errors) and even fire engine analytics (`MiscEvent SwampyPressOnLevel`), so events ARE delivered into the gameplay scene — but carve/pause actions don't fire. Tell: after a webOS task-switch the app auto-pauses into a UI scene that IS tappable; Resume -> untappable gameplay again. So it's the gameplay scene's INPUT PATH, not delivery/coords. Ruled out: coordinates (UI uses same normalized path, works), sRealScreenSize (was (0,0); mirrored to (768,1024) @ base+0x4705bc -> no effect). libwmw: 0 pthread_create but uses pthread_mutex + pthread_cond_wait/broadcast -> threads are Android's (UI thread onTouchEvent, GL thread onDrawFrame); gameplay likely drains a touch queue across that boundary that our single-thread shim doesn't satisfy. NEXT: instrument libwmw (bake log writes into the apk at rendererTouchBegan's receiver check @file-off 0x10f51c + the scene input dispatch), run normally, compare Screen_WaterTest vs Screen_LevelSelect touch. AVOID gdb-over-SSH: it kept silently detaching AND the gdb-launched instance received 0 taps (no input). **RESUME STATE:** device clean; apkenv SOURCE in scratchpad/apkenv/ (modules/wheresmywater.c, platform/webos.c); current build has PDL-before-SDL+aggression/gestures, normalized touch fx=(768-rawY)/768 fy=rawX/1024, finger=0 force, touching-gate, sRealScreenSize mirror, screen rotation via current_orientation=PORTRAIT + gles_viewport_hack + glOrthof_rotation_hack. Device: /media/internal/wheresmywater.apk (fixed apk, libwmw cdf477f1 baked in), /var/apkenv/{apkenv,libs,data/lib,play.sh,stop.sh}. Run: `sh /var/apkenv/play.sh`. SSH: root@192.168.10.88 (legacy-cipher opts). **Full writeup: project file `android-port-shim.md` §6c.**

**UI TOUCH SOLVED (2026-06-26): WMW menus interactive (tap a button -> level loads).** Two bugs in series, both via leads from the user (3-layer compositor + read ACL/Java touch path): (1) **PDL_Init MUST be before SDL_Init** (+ `PDL_SetTouchAggression(PDL_AGGRESSION_MORETOUCHES)` + `PDL_GesturesEnable(PDL_FALSE)`); wrong order -> malformed events through the TouchPad's 3-layer compositor -> inconsistent finger ids -> engine logs `touchMoved ERROR - moved finger not found in map!`. (2) **The engine wants NORMALIZED 0..1 touch coords, not pixels.** Found by `baksmali classes.dex` -> `WMWView.copyTouches` does `getX(i)/getWidth()`, `getY(i)/getHeight()` before `rendererTouchBegan(count, xs[], ys[], ids[])`. Sending pixels (~hundreds) hit-tests hundreds of screens off -> "nothing registers anywhere" (looks like events not reaching the game; they ARE - wrong coordinate SPACE). Final transform (portrait, 1024x768 surface, inverting apkenv's 90-deg glOrthof rotation, corner-calibrated): `fx=(768-rawY)/768`, `fy=rawX/1024`. Also: gate MOVE on a finger being down (SDL emits motion w/ no button) and force a constant finger id for single-touch. Lesson: when sure "it's not coordinates," check the coordinate SPACE (normalized vs pixel) before concluding dispatch is broken. webOS MCP `webos://knowledge/pdk` (3-layer compositor + PDL touch API) was decisive. **Full writeup: project file `android-port-shim.md`.**

**SCREEN ORIENTATION cracked (2026-06-26): WMW is a PORTRAIT game** (engine calls glViewport(0,0,768,1024) every frame; renders a 768x1024 portrait buffer). The "70% left strip" was the portrait render sitting in the left of our 1024x768 LANDSCAPE surface. Mechanics: apkenv wraps GL (engine's gl* resolve to my_gl* even via eglGetProcAddress = apkenv_get_hooked_symbol). apkenv rotation model: module sets `GLOBAL_M->module_hacks->current_orientation` (LANDSCAPE=0, PORTRAIT=1 in apkenv.h), platform `get_orientation()` = device natural; when they DIFFER, my_glViewport (only if `gles_viewport_hack=1`) and my_glScissor (always) swap (x,y,w,h)->(y,x,h,w) and input_transform rotates. Tried: (a) current_orientation=LANDSCAPE + gles_viewport_hack=1 + get_orientation=PORTRAIT -> viewport 768x1024->1024x768 FULL but STRETCHED (swap != true rotation; portrait squished to landscape). (b) Portrait SDL surface SDL_SetVideoMode(768,1024) [+PDL_SetOrientation(90)] -> BLACK: TouchPad PDK framebuffer is hardwired landscape 1024x768; portrait GL surface renders (loading flashes) but compositor won't present a non-native surface. PDL_SetOrientation took effect (no landscape-revert on kill) but doesn't change the FB. CONCLUSION: clean portrait fill needs a **90-degree render-rotation**: render engine to an offscreen 768x1024 FBO (redirect engine glBindFramebuffer(0)->myFBO), then webos_update draws a rotated textured quad to the real 1024x768 FB (GLES1 fixed-function; device has GL_OES_framebuffer_object); also rotate tap coords. NOT YET BUILT. Baseline now: native landscape surface, current_orientation=LANDSCAPE, no hacks => portrait game visible in left ~75%, native aspect, runs clean, 0 crashes.

**Device:** HP TouchPad "FreshPad", webOS, Linux 2.6.35-palm-tenderloin armv7l, NEON/vfpv3. novacom id topaz-linux. webOS GL stack present: `/usr/lib/{libGLESv2,libEGL,libpdl,libSDL-1.2,libSDL}.so`; **GLES1 is `libGLES_CM.so`** (old name — bridge bionic libGLESv1_CM imports to it). `/media/cryptofs` is fuse (exec ok for scripts, avoid for binaries); `/var` and `/` are real ext3 — run binaries there.

**Spike target = Where's My Water** (`wheresmywater_1.0.2.apk`): native Walaber engine, NO Mono, GLES1 (libGLESv1_CM, 48 gl* calls), FMOD audio (libfmodex, AudioTrack backend). EGL is host-side (zero egl imports). Outbound JNI = 1 stubbable class `com/disney/DMO/DMOAnalyticsEngine`. ~144 bionic libc imports to shim. **14 driving JNI entrypoints**: WMWRenderer.{rendererInit, rendererDrawFrame, rendererResized, notifyScreenResized, rendererReloadContextData, rendererTouchBegan/Moved/Ended}; BaseActivity.{onGamePause, onGameResume, onLostFocus, onRegainedFocus, accelerometerChanged, backKeyPressed}. Host loop: init→resize→loop(drawFrame+eglSwap), feed touch/accel, lifecycle.

**KEY RESULT (strace):** harvested ACL bionic `linker` executes on the kernel and **`set_tls()` returns 0** → ARM/kernel TLS ABI is bionic-compatible, bionic code CAN run on TouchPad in a normal process. BUT running the Android `linker` **standalone** (`linker <prog>`) **null-derefs (SIGSEGV @0) right after set_tls** — the standalone-bootstrap path is broken in this build. So:

**LOADER ARCHITECTURE DECISION: use the libhybris approach** — compile a patched **gingerbread linker as a library** into a glibc (PalmPDK) host and call `android_dlopen()` from webOS-native code; never exec the Android linker standalone. ACL image is Android 2.3.6 = gingerbread, matching libhybris's gingerbread linker; harvested libc/libm/liblog are the runtime it loads. Next offline step: port libhybris gingerbread linker to PalmPDK arm-gcc (4.3.3/glibc2.4). Then graphics = host creates webOS PDL/EGL context, resolve engine gl* imports to webOS GLESv2/GLES_CM (value-in/out, trivial marshal). Build wrapper as a normal PDK .ipk (runs only when launched — unlike ACL).

**Candidate ranking** (thin no-Dalvik wrapper): WheresMyWater ⭐ > WheresMyWater2 > BejeweledBlitz (native SexyAppFramework, armeabi-only) > FruitNinja (native Mortar, big ad JNI) > TempleRun2 (Unity+Mono JIT unknown) > CutTheRope2 (7MB dex) > FlappyBird (AndEngine = Java/Dalvik, avoid).

## apkenv webOS port — BUILDS & RUNS on device (scratchpad/apkenv)
Chose **apkenv** (github.com/thp/apkenv) as the base — it already is the architecture (gingerbread bionic linker-as-library + fake-JNI + platform backends + per-game modules). Wrote `platform/webos.c` (+ webos.mk) = harmattan.c minus X11 + PDL_Init; SDL_OPENGLES + SDL_GL_SwapBuffers (webOS GL via SDL/PDL, no raw EGL). Bionic libs in `libs/webos/` (reuse apkenv's gingerbread libc/libm/libstdc++ + harvested liblog/libz).

**Cross-build recipe (hard-won — `build-webos.sh`):** PalmPDK gcc 4.3.3 is too old (rejects GLES1+GLES2 duplicate typedefs that all GL headers trigger). Solution = **two-toolchain**: COMPILE with host `arm-linux-gnueabi-gcc-13` (tolerates dup typedefs) but force old glibc via `-nostdinc -isystem <gcc13 builtins> -isystem /opt/PalmPDK/arm-gcc/sysroot/usr/include` (else it pulls glibc 2.38 → `__isoc23_*` syms + GLIBC_2.38 the device lacks); add `-fgnu89-inline` (old-glibc extern-inline → multiple-def of ferror_unlocked otherwise), `-fno-builtin -fno-stack-protector -D_BSD_SOURCE`, `-include compat/pdk_compat.h` (declares bsd_signal/__vsprintf_chk), `-I glshim` FIRST (modern Khronos GLES/EGL/KHR headers from host libgles-dev). Patched `compat/gl_types.h` (APKENV_PDK_GL branch, unused now). Then **LINK with PalmPDK gcc 4.3.3** (binds glibc 2.4 syms) + `-lrt -lEGL -lm`; pulled device `/usr/lib/libEGL.so` into devlibs/ (not in PDK SDK). Result: 418KB ARM, highest sym **GLIBC_2.4**, interp /lib/ld-linux.so.3 — device-clean.

**Bug fixed in apklib.c** `apk_get_shared_libraries`: it `fopen`'d zip *directory* entries (lib/armeabi-v7a/) for writing, failed, and `break`'d the whole loop → "Not a native APK". Fixed: skip entries ending in '/', and `continue` not `break` on fopen fail.

**STATUS (huge progress): WMW renders 3 frames then crashes in game logic.** apkenv runs on TouchPad, links all device libs, loads libwmw+libfmodex. Added `setvbuf(_IONBF)` to main for live logs. Wrote **`modules/wheresmywater.c`** (MODULE_PRIORITY_GAME) driving the real entrypoints — got JNI signatures via `baksmali` of classes.dex: WMWRenderer.rendererInit(String sourceDir=apkPath, String dataDir=home, Context), rendererResized(II), notifyScreenResized(II), rendererDrawFrame(), rendererTouch{Began/Moved/Ended}(I[F[F...[I), BaseActivity.{onGamePause/Resume,onLost/RegainedFocus,accelerometerChanged(FFF),backKeyPressed}. Module init = JNI_OnLoad → rendererInit(apk_filename, home, GLOBAL_M) → rendererResized+notifyScreenResized(w,h) → onGameResume; update = rendererDrawFrame; touch stubbed for now. Also patched apklib.c (skip zip dir entries — was breaking on `lib/armeabi-v7a/`).

**Confirmed working end-to-end:** bionic load, fake-JNI, engine's own ZipArchive reads assets from the apk (textures/xml), GL context 1024×768, **3 frames rendered+swapped**. Then crash: SIGSEGV @0x4 in `WaterConcept::Swampy::update(float)` = `ldr r0,[r0,#4]` with this=NULL. Reconstructed call chain via webOS libgoodabort stack-scan (libwmw base 0x2c114000; map on-stack addrs in range → file offsets → objdump): `Screen_MainMenu::update → _goEnter → _buildUI → WidgetHelper::loadWidgetsXML` → crash updating null **Swampy** (the bathtub scene shown behind the main menu). Intro plays 3 frames, then entering Main Menu the Swampy background concept is null.

**Asset-path bug FOUND + redirect works (root cause of "no root element" understood):** engine has 2 file paths — preloaded textures via its own ZipArchive (reads the apk, works), but on-demand loads use `fopen()` with ABSOLUTE paths (`/Textures/...`, `/Data/tweakables.csv`) that hit `/` and fail (on Android the resource root resolves these into the app's asset dir). Added redirect in apkenv `my_fopen`/`my_open`: on failure with a leading-`/` path, retry under `getenv("APKENV_ASSET_ROOT")`. Extracted apk `assets/*` (strip `assets/` prefix) to `/media/internal/gameroot` (data-only; `/var` is only 62MB and filled up — keep big data on /media/internal). VERIFIED redirect fires: `/Data/tweakables.csv -> .../gameroot/Data/tweakables.csv : OK`. The 4 MISSes (`collectible_screen.imagelist`, `wmw_logo.png`) are genuinely absent because this is **WMW *Lite*** (apk has `wmw_logo-Lite-*`, `collectibles_promo` instead) — benign, NOT the crash cause. User wants this done as a webOS-friendly REPACK (ship pre-extracted assets, don't unpack at runtime); use a hidden dir like `/media/internal/.apkenv/<pkg>` (similar to ACL's `/media/internal/.android`, not same).

**Debugging infra (for cracking engine-internal bugs):** device has `/usr/bin/gdbserver` (6.8, Sourcery) + libthread_db. Host: no sudo, but `apt-get download gdb-multiarch` + `dpkg-deb -x` gives a working client (scratchpad/gdbpkg). BLOCKED on connection: `novacom -P -f 2345:2345` → "socket open failed" (webOS may only forward registered services); device eth0=192.168.10.88 pingable but slow (1.2s) and TCP 2345 firewalled between host subnet (192.168.134.x) and device. webOS uses "minicore" not standard cores (apkenv blocks ~60s on crash). libwmw loaded by apkenv's own linker at fixed base **0x2c114000** (runtime addr = 0x2c114000 + file vaddr; verified: crash PC 0x2c3d1c14 = base+0x2bdc14 = Swampy::update+0x10). For gdb: `add-symbol-file libwmw.so -o 0x2c114000`. NEXT debug options: (a) get gdbserver reachable (needs working forward/route), (b) in-process inline-hook logging from the module (patch fn entries → trampoline → fprintf), (c) webOS minicore dump → offline gdb.

**WMW menu blocker (precise):** `Screen_MainMenu::update(float,bool)` @0x388360: if bool!=0 AND entered-flag `+400`!=0 → runs `Swampy::update(+164)` then `ParticleSet::updateParticles` etc. on the scene objects. First entry (+400==0) → init path @0x388a00 which sets `+400`=1 @0x389254 then calls `_goEnter` @0x389274. `_goEnter` @0x38339c has NO early-return guard and stores `+164`=getInstancePtr() (lazily-creates Swampy) @0x38446c. Yet at crash +400!=0 but +164 AND particleset are null → the scene-init (_goEnter) set +400 but its object setup didn't take effect (or ran on a different instance / got torn down by `_curtainExit` @0x389284). NOP'ing one crash site just advances to the next null subsystem (whack-a-mole) — the whole menu scene is uninitialized. Needs a debugger/instrumentation to see why _goEnter's setup doesn't stick.

**FIXED — menu renders, no crash (2026-06-26).** Root cause via gdb: `Screen_MainMenu::_goEnter()` calls `_buildUI()` → `WidgetHelper::loadWidgetsXML("/Data/SN_MainMenu-Lite.xml",...)` which **throws** `std::logic_error` and unwinds past `_goEnter`'s Swampy(+164) store, leaving the scene null → next-frame `Swampy::update(null)` crash. The throw is `std::string` constructed from a **NULL char\*** inside `ndk::ApplicationContext::messageRx()` (and also `rendererInit`, which was caught). libwmw **statically links libstdc++**, so `_S_construct<char const*>` (at file off 0x3f88dc) does the `null not valid` throw. **Fix = 1-instruction binary patch** to libwmw at file offset **0x3f891c**: `0x0affffe9` (beq 0x3f88c8 → throw-check) → `0x0affffe5` (beq 0x3f88b8 → empty-string path), so `std::string(NULL)` yields `""` instead of throwing. After patch: apkenv runs WMW main menu sustained (18s, ~10s CPU = active render loop), no SIGSEGV. Patched lib = scratchpad/libwmw_fixed.so. **Touch input now implemented** in modules/wheresmywater.c (rendererTouchBegan/Ended(I[F[F[I), rendererTouchMoved(I[F[F[F[F[I) — builds 1-elem JNI float/int arrays via apkenv fake-JNI NewFloatArray/SetFloatArrayRegion; coords from input_transform; tracks last_x/y for Moved). Touch is WIRED BUT UNTESTED (needs physical taps; user was away). On device: `/var/apkenv/play.sh` launches it (nohup), `/var/apkenv/stop.sh` kills. Staged: /var/apkenv/apkenv (touch build), data/lib/armeabi-v7a/libwmw.so (=libwmw_fixed.so), assets on /media/internal/gameroot. NEXT: confirm menu visually + test touch (tap buttons); then repackage as a proper webOS .ipk (bake the libwmw std::string patch into the apk, ship apkenv+libs+assets, set APKENV_ASSET_ROOT to a hidden dir like /media/internal/.apkenv/<pkg>).

**Debugging is via gdb (now working).** SSH to device works: `ssh -oKexAlgorithms=+diffie-hellman-group1-sha1 -oCiphers=+aes128-cbc,3des-cbc -oHostKeyAlgorithms=+ssh-rsa,ssh-dss -oPubkeyAcceptedAlgorithms=+ssh-rsa,ssh-dss -oMACs=+hmac-sha1 -i ~/.ssh/id_rsa root@192.168.10.88` (VM moved to 192.168.10.x subnet; optware openssh on device; my key in /home/root/.ssh/authorized_keys). For gdb: run `gdbserver 0.0.0.0:2345 ./apkenv wmw.apk` on device, **SSH tunnel `-L 2345:127.0.0.1:2345`** (NOT `localhost` — device /etc/hosts only has `localhost.local`!), then `gdb-multiarch` (scratchpad/gdbpkg) with `set auto-solib-add off; set sysroot <emptydir>; file apkenv; target remote 127.0.0.1:2345; break wheresmywater_init; continue; add-symbol-file libwmw_orig.so -o 0x2c114000`. webOS has iptables (only port 22 open) so direct TCP to other ports fails — must tunnel via SSH. gdbserver 6.8 (no hw watchpoints, no stdio mode).

**CRITICAL gotcha (cost hours of confusion): `gdbserver` does NOT pass env vars (APKENV_KEEP_LIBS, APKENV_ASSET_ROOT) to the inferior** → apkenv re-extracts the ORIGINAL libwmw from the apk, OVERWRITING the patched /var/apkenv/data/lib/.../libwmw.so → reverts the std::string fix → Swampy/menu crash returns. FIX: **bake the patch into the apk** (scratchpad/wheresmywater_fixed.apk = original apk with lib/armeabi*/libwmw.so replaced by libwmw_fixed.so via python zipfile; md5 cdf477f1f396a08c5878739e3515998b). Deployed to device /media/internal/wheresmywater.apk. Now extraction always yields the fixed lib (stable, 0 crashes). Keep big data (apk+gameroot) on /media/internal, exec-only (apkenv+libs+extracted .so) on /var (small, 62MB). play.sh runs `./apkenv /media/internal/wheresmywater.apk`.

**Open issues after menu works (2026-06-26): (1) no touch response, (2) no sound, (3) screen only fills ~70% (left-aligned, native aspect, no scale-up).** Diagnosed #3 via gdb: engine renders at `Walaber::ScreenCoord::sScreenSize` = **(768,1024) PORTRAIT** (should be (1024,768)) into the 1024×768 GL viewport at 1:1 → 75%≈70% left region. `sRealScreenSize`=(0,0). `GameSettings::CurrentDeviceOrientation`=2. Globals (file offsets): sScreenSize 0x4705a4, sRealScreenSize 0x4705bc, CurrentDeviceOrientation 0x46f97c (runtime = +0x2c114000). Chain: rendererResized(1024,768)→`ndk::ApplicationContext::setDisplaySize` (stores displaySize 1024×768 correctly at obj+0x4c/0x50, NO swap)→`initializeGame()` @0x10982c computes sScreenSize as portrait (the swap/orientation logic is a tangle — platform-tags vector + per-case setup; libwmw makes ZERO glViewport/glOrtho calls, uses Walaber::Camera + SpriteBatch::setCameraProjectionMatrix). notifyScreenResized is a no-op (bx lr). EXPERIMENTS: (a) passing pre-swapped (768,1024) to rendererResized → CRASH (displaySize must match real surface). (b) gdb force sScreenSize=(1024,768) live → briefly STRETCHED TO FILL then engine recomputed back to 70% then crashed (camera cached old size). CONCLUSION: sScreenSize is the lever and IS read for rendering, but it's recomputed + the Camera is built from it at init → must fix the ORIENTATION/size INPUT before initializeGame, not the output. Touch (#1) likely tied to same (Camera::screenToWorld maps taps via sScreenSize). NEXT: find what makes initializeGame pick portrait (orientation source / platform tags from PlatformManager::getPrioritizedPlatformTags) and force landscape; OR patch initializeGame's sScreenSize computation to (1024,768). Sound (#2) = separate FMOD(libfmodex)→webOS audio bridge (FMOD uses AudioTrack JNI backend; not started).

**(historical) Crash is NOT file I/O.** It's `Screen_MainMenu+164` (the Swampy bathtub-background ptr) never assigned → `Swampy::update(null)` on frame 3 (a countdown at `Screen_MainMenu+424` expires then activates Swampy). The field is stored at libwmw `0x38446c` (`str r0,[sl,#164]`). `loadWidgetsXML(filename, WidgetManager*, SharedPtr<Callback>)` is async/callback-based; suspected the widget-load completion callback that assigns +164 isn't firing (libwmw has NO pthread_create — loader is likely a deferred queue; or callback fired with failure). NEXT lead: find what fn contains 0x38446c and what gates it / whether the async callback runs. Cleaned /var after testing.

**Lead (resolved, benign):** `ERROR! no root element in XML` parsing `collectible_screen.imagelist` (found, 6369 bytes uncompressed, but parser sees empty) — other XMLs (Swampy.xml, sounds.xml, SN_MainMenu.xml) parse OK. Suspected webOS-specific file-read/decompress/null-termination issue feeding the engine's libxml2; likely cascades to the null Swampy. NEXT: trace the engine's read of textureSettings.xml (buffer empty vs not-null-terminated); check apkenv read_file/mmap path; then re-test. Build recipe + run loop all in scratchpad/apkenv (`build-webos.sh`); run dir on device `/var/apkenv` (real ext3, exec ok; NOT /media/cryptofs fuse). Device GL window presents even from novacom console (user saw fullscreen app) — no .ipk needed yet to see output.
